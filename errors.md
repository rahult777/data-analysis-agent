# Errors Log

Read this file at the start of every session before writing any code.

## Format

`Date | Error | Root cause | Solution`

---

<!-- Add entries below as errors are encountered and resolved -->

**2026-04-28 | anthropic==0.25.0 TypeError: Client.__init__() got an unexpected keyword argument 'proxies'**
Root cause: anthropic==0.25.0 requires httpx<0.25.0 but supabase==2.28.3 requires httpx>=0.26, causing a version conflict. The proxies argument was removed in newer httpx versions.
Solution: upgraded anthropic from 0.25.0 to 0.97.0 which supports httpx>=0.25.0, compatible with supabase's requirements. Updated requirements.txt accordingly.

**2026-05-06 | FutureWarning: sns.boxplot with all-NaN column and pandas 2.2**
Root cause: seaborn internals emit a FutureWarning when sns.boxplot receives a column that is entirely NaN values, triggered by pandas 2.2 internal behavior change. The except block in generate_boxplot catches this safely and returns None without crashing.
Solution: already handled gracefully by the try/except pattern in viz_tools.py. No code change needed. Be aware this warning may appear in logs when datasets have all-NaN numeric columns after cleaning.

**2026-05-07 | PauseResumeRequest fields corrupted by Code Review plugin**
Root cause: Code Review plugin changed response: dict to action: str and user_input: Optional[str] without being instructed to. The resume endpoint expects a dict containing the full user decision to write to user_pause_response in the database.
Solution: follow-up fix commit (9388400) restored PauseResumeRequest to exactly one field: response (dict). Verification: python -c 'from backend.models.schemas import PauseResumeRequest; print(PauseResumeRequest.model_fields)' must print only response field.

**2026-05-15 | test_analyzer.py sepal_width and petal_width excluded from numeric_cols on iris.csv**
Root cause: classify_columns excludes any column whose name contains "id" as a substring — "width" contains "id" so sepal_width and petal_width are excluded from numeric_cols on iris.csv. This is documented behavior not a bug in the code.
Solution: test_classify_columns_numeric updated to assert sepal_length and petal_length are present and sepal_width and petal_width are absent, with an explanatory comment. Any future fixture or test involving columns with "id" anywhere in the name must account for this exclusion.
Status update (2026-09-19, Build B): the "documented behavior" was the bug, and it is now FIXED, committed in 4b980d3. The substring rule also silently dropped valid, paid_amount, humidity, covid_19_cases and similar columns. classify_columns now excludes a numeric column only when "id" is a standalone name component, via the private helper `_is_id_column` in analyzer.py (separator split plus camelCase boundary; exact token match). On iris.csv all 4 measurements are now numeric. test_classify_columns_numeric was flipped to assert sepal_width and petal_width are INCLUDED, and test_classify_columns_id_excluded (customer_id) still passes. The instruction above about accounting for this exclusion no longer applies: only a standalone "id" component (customer_id, CustomerID, id_customer, …) is excluded now. Four accepted limitations (IDCustomer/CustomerIDNumber, uuid/guid, id1/related_ids, userid/customerid) are listed in decisions.md 2026-09-19 and locked in by tests. Full suite 176 passed / 16 skipped.

**2026-05-17 | Build error: `border-border` class does not exist in globals.css:3:1**
Root cause: globals.css contained `@apply border-border outline-ring/50` in the universal `*` selector. `border-border` is a shadcn/ui shorthand that requires Tailwind v4. The project uses Tailwind v3 (tailwind.config.ts present), where this utility does not exist as an @apply-able class. This was a pre-existing issue in the frontend scaffold that was exposed on first compile.
Solution: Replaced `@apply border-border outline-ring/50` with the Tailwind v3 equivalent using explicit CSS custom property reference.

**2026-06-03 | Pipeline SYSTEM_ERROR "Failed to parse model response as JSON: Unterminated string starting at line 354 column 24 (char 29287)" — Analyzer LLM output truncated at max_tokens**
Context: First true end-to-end pipeline run on real LLM calls (prior runs were unit tests with mocked responses). Triggered during the Results-page Phase 1 verification gate, uploading tests/fixtures/iris.csv. Pipeline reached Profiler (ok) -> Cleaner (ok) -> Analyzer (FAILED); Explainer never ran.
Root cause: The Analyzer's synthesis call used max_tokens=8000 (backend/agents/analyzer.py). The analyzer's output contract makes the LLM echo back the full computed stats blocks — descriptive_stats per column (with verbose confidence_reasoning), correlation_matrix, distributions, value_counts — which Python then OVERWRITES with its own deterministic values at analyzer.py:740-746. So the LLM spends thousands of output tokens regenerating data that is immediately discarded. On iris.csv (only 15 rows; small-n made the model verbosely hedge every field with "n=15, well below the 50-row floor") the response exceeded 8000 tokens and was cut off mid-string. parse_json_response (backend/agents/profiler.py:124) then failed json.loads and re-raised as SYSTEM_ERROR. No stop_reason=="max_tokens" check existed, so truncation surfaced as a cryptic JSON parse error instead of a clear "response truncated" message. (Charts WERE generated successfully before the failure — 10 PNGs + 1 scatter HTML on disk — but chart_paths/analysis_report were never saved to the DB because the Analyzer errored before its save step, and executive_summary/insight_report were never produced.)
Solution (applied, NOT yet validated end-to-end): In analyzer.py, added module constant ANALYZER_MAX_TOKENS = 32000, used it in the messages.create call (was max_tokens=8000), and added a guard that raises a clear ValueError when response.stop_reason == "max_tokens". Module verified to import cleanly. The re-run that would validate this was deferred (session ended before re-running). Anthropic SDK non-streaming default timeout is 10 min (verified via Context7); 32000 is only a ceiling, not forced output, so it won't approach the timeout for iris.
Pending follow-ups: (1) APPROVED BUT NOT YET APPLIED — the Explainer (backend/agents/explainer.py:127) has the identical max_tokens=8000 and a large 3-layer output (5 executive bullets + analyst narrative + technical methodology/code blocks); apply the same fix (raise to 32000 + stop_reason guard) BEFORE the next gate run to avoid a second truncation failure. Its smaller custom-question calls (max_tokens=2000/1000) are not in the gate path. (2) Deeper fix (real root inefficiency, out of scope so far): stop instructing the Analyzer to echo the computed stats blocks in its JSON since Python overwrites them — would shrink analyzer output substantially. (3) Profiler and Cleaner completed fine at max_tokens=8000 in this run. (4) Unrelated observation: tests/fixtures/iris.csv is a 15-row stub, not the 150-row dataset described in docs/infrastructure.md:282 — docs are inaccurate; tests are self-consistent (assert row_count == len(iris_df)).
Status update (2026-09-25, documentation cleanup): follow-up (1) was APPLIED on 2026-06-04, committed in 2e7b957: the Explainer's main synthesis call got `EXPLAINER_MAX_TOKENS = 32000` and the `stop_reason == "max_tokens"` guard, and both main synthesis calls (Analyzer and Explainer) were switched to streaming, which the 32000 ceiling requires (next entry). The pipeline first completed end to end the same day, 2026-06-04 (see the GET /api/analysis/{id} 500 entry below). Follow-up (2) was done on 2026-09-17 in 68af76e (the Analyzer echoed-stats trim in analyzer_system.md). Note (4) is CLOSED: tests/fixtures/iris.csv has 15 data rows (confirmed with pandas), and docs/infrastructure.md now says 15 rows. docs/agents/profiler.md:192 also said 150 rows and now says 15 rows too.

**2026-06-04 | Pipeline SYSTEM_ERROR "Streaming is required for operations that may take longer than 10 minutes" — main synthesis call rejected client-side at max_tokens=32000**
Context: Phase 1 gate re-run after applying the 2026-06-03 fix (ANALYZER/EXPLAINER_MAX_TOKENS=32000 + stop_reason guard). Uploaded tests/fixtures/iris.csv. Pipeline reached Profiler (ok) -> Cleaner (ok) -> Analyzer FAILED at backend/agents/analyzer.py:687 (the main messages.create call). Explainer never ran but carried the identical latent defect.
Root cause: The Anthropic SDK computes the non-streaming request timeout from the max_tokens CEILING, not actual output. In anthropic/_base_client.py:_calculate_nonstreaming_timeout: expected_time = 3600 * max_tokens / 128000; if expected_time > 600s (i.e. max_tokens > ~21,333) it raises ValueError("Streaming is required...") BEFORE any HTTP request. 8000 -> 225s (why Profiler/Cleaner and the old code passed); 32000 -> 900s (rejected). The 2026-06-03 note claimed "32000 is only a ceiling, not forced output, so it won't approach the timeout" — that reasoning was WRONG: the SDK caps on the ceiling regardless of real output. The rejection is client-side, so the Analyzer call cost $0 (never reached Anthropic); only Profiler+Cleaner billed, making this failed run cheaper than the prior $0.74 run.
Solution (applied; imports + unit tests clean; end-to-end gate validation pending): Switched BOTH main synthesis calls to streaming — backend/agents/analyzer.py:~687 and backend/agents/explainer.py:~133 now use `with client.messages.stream(...) as stream: return stream.get_final_message()` (wrapped in asyncio.to_thread). messages.stream() lifts the non-streaming 10-min guard (per the Anthropic SDK long-requests guidance / claude-api skill streaming doc). get_final_message() returns the same Message object messages.create() did, so the existing stop_reason=="max_tokens" guard and parse_json_response(response.content[0].text) are unchanged. max_tokens stays 32000 (under Sonnet 4.6's 64K streaming output cap); model unchanged (claude-sonnet-4-6). The smaller custom-question calls in explainer.py (max_tokens=2000/1000) remain non-streaming (well under the ~21.3K threshold). Verified: both modules import cleanly; 40 unit tests pass / 5 integration tests skipped. NOT yet validated end-to-end — the next gate re-run will confirm the full Profiler->Cleaner->Analyzer->Explainer path on real LLM calls.
Status update (2026-09-25, documentation cleanup): VALIDATED end to end on 2026-06-04, committed in 2e7b957. With both synthesis calls streaming, the Profiler → Cleaner → Analyzer → Explainer pipeline completed on iris.csv for the first time that day; the read-endpoint 500 that this first completion exposed is the next entry.

**2026-06-04 | GET /api/analysis/{id} 500 — pydantic ValidationError: 15 errors for AnalysisResponse on the first real completed analysis**
Context: The streaming fix let the pipeline complete end-to-end for the first time (iris.csv). The read endpoint then 500'd — it had never run against a populated row before. main.py:221 builds AnalysisResponse from the analyses row's JSONB columns.
Root cause: The strict nested API models did not match the raw agent-output JSONB the agents store. profile_report (ProfileReport wants row_count/column_count/columns; stored has domain_hypothesis/column_profiles/top_3_concerns/... — 16 keys), analysis_report (AnalysisReport wants descriptive_stats/distributions/value_counts as lists; stored as dicts keyed by column + a separate correlation_matrix — 17 keys), insight_report (FullInsightReport wants data_overview/key_findings/...; stored is layer-based executive_layer/analyst_layer/technical_layer/open_questions/...), executive_summary (ExecutiveSummary wants bullet_points; stored is {bullets:[...]}). This is the read-side counterpart of the 2026-05-07 raw-dict-save decision — the agents write raw LLM-contract dicts to JSONB.
Solution: Relaxed those four AnalysisResponse fields to `dict | None` (pass-through), leaving cleaning_report/cleaning_decisions strictly typed (they validated fine and were not among the 15 errors). GET re-tested -> HTTP 200 with the full payload (5 executive bullets, 3 insight layers, 11 chart_paths, data_quality_score 0.9). See decisions.md 2026-06-04 for design rationale and the Phase 2 frontend note.

**2026-09-17 | Profiler (and derivatively Cleaner) reports full-dataset statistics that are wrong when the uploaded file is sorted/grouped by a categorical column**
Confirmed via live diagnostic on analysis_id 9e5c7266-b31d-4183-a067-99977375fc17: profile_report and cleaning_report described a 15-row single-species dataset, but the actual uploaded/cleaned data was a balanced 3-species 15-row file (rows grouped in blocks: 0-4 setosa, 5-9 versicolor, 10-14 virginica). Cross-verified via direct Supabase MCP read of the stored analyses row, an independent fresh download of the parquet from Storage, and the Analyzer's own correctly-computed stats — all three agreed with each other and disagreed with profile_report/cleaning_report.
Root cause: build_profiler_message (profiler.py) sends the LLM only df.head(5) as first_5_rows and non_null_vals.head(3) per column as sample_values, with no full-column aggregate statistics (mean, std, min, max, unique_count, outlier stats) computed by Python and passed in as trusted context. When a file is grouped/sorted by category, this head-based peek is unrepresentative of the whole dataset, and the Profiler's LLM extrapolates false full-dataset claims from it (e.g. "zero-variance species column" when the full file has 3 species). Confirmed Profiler-origin: Cleaner's own numeric computations (missing_pct, IQR outlier bounds) remain correct because they use full pandas column operations, not head() — only Cleaner's narrative text and its own sample_values line inherit the same head(3) flaw independently. Analyzer's output was entirely correct (its compute_descriptive_stats/compute_value_counts/compute_correlation_matrix use zero row-sampling) and its LLM correctly flagged the contradiction it inherited via Memory MCP context as its most_surprising_finding — the system's honesty mechanism worked as designed.
Solution: Diagnosed and planned via Sequential Thinking MCP this session; NOT yet implemented. See decisions.md (2026-09-17 entries) for the three scope decisions made. Plan: add compute_column_stats() to profiler.py computing unique_count/mean/std/min/max/outlier_count/outlier_pct deterministically over full columns, passed to the Profiler's LLM as trusted input (same pattern as tonight's Analyzer fix — Python computes, LLM reasons over computed truth). Redesign sample_values sourcing: categorical/object columns via series.dropna().drop_duplicates().head(5) instead of raw head(); numeric/datetime via series.sample(min(5,len(series)), random_state=42) instead of head(). Apply the equivalent sample_values fix to cleaner.py's build_cleaner_message independently (same flaw, separate occurrence). Update profiler_system.md Step 3 to state sample_values is illustrative only, never a source of a quantitative or cardinality claim. Explicitly confirmed NOT caused by and unrelated to the 2026-09-17 Analyzer echoed-stats trim (analyzer_system.md) completed earlier this session — different files, different mechanism, verified by reading the actual code paths.
Status update (2026-09-17, build session): IMPLEMENTED and unit-tested; live end-to-end validation not yet run at this point (completed 2026-09-18 — see the next status update). compute_column_stats was added to profiler.py and is sent to the LLM as a top-level `computed_column_stats` key; sample_values sourcing is fixed in both build_profiler_message and build_cleaner_message; profiler_system.md Step 3 now tells the LLM to copy the computed stats verbatim, treats sample_values as illustrative only, and no longer tells it to compute IQR outliers itself. 13 new tests (12 in test_profiler.py, 1 in test_cleaner.py); full suite 108 passed / 16 skipped; all 16 in-memory mutants of the new code were caught by the new tests. Open Code Review follow-ups, not yet approved: (1) profiler_node saves the LLM's column_profiles as-is — nothing checks or overwrites the copied stats with compute_column_stats' values after parsing, so a mis-copied number would persist and reach the Analyzer and Explainer; (2) bool columns still take the random-sample branch of the sample_values code (is_numeric_dtype accepts bool), so a heavily imbalanced True/False column can still show a homogeneous sample.
Status update (2026-09-17, overwrite-guarantee build): follow-up (1) RESOLVED. profiler_node now calls apply_computed_column_stats after parsing and before the profile_report save. It replaces each `column_profiles` entry's unique_count/mean/std/min_value/max_value/outlier_count/outlier_pct (from `computed_column_stats`) and dtype/missing_count/sample_values (from `column_info`) with Python's values; missing_pct stays LLM-owned because `column_info` has no such field. 3 new tests in test_profiler.py (overwrite, malformed-output robustness, mocked node-level save); full suite 111 passed / 16 skipped; all 12 in-memory mutants of the new code were caught. Live end-to-end validation: COMPLETED successfully on 2026-09-18, after both the row-sampling fix and the overwrite guarantee were built — analysis_id 1a96db4a-3c88-460d-a39a-820b619536a4 on iris.csv. The Profiler reported species with 3 unique values (5 setosa, 5 versicolor, 5 virginica), matching the Analyzer's independently computed values. This is the run cited in commit 976200b's message; the fix was committed immediately afterward (recorded from the user's confirmation and that commit message). See decisions.md 2026-09-17 (overwrite guarantee). Follow-up (2) remains OPEN: bool columns still take the random-sample branch of the sample_values code (is_numeric_dtype accepts bool), so a heavily imbalanced True/False column can still show a homogeneous sample. It is a separate bug from the Cleaner bool-dtype TypeError crash in the next entry and has its own tasks.md line.
Status update (2026-09-19, Build A): follow-up (2) RESOLVED, committed in 4961e9f. The sample_values condition in build_profiler_message (profiler.py:149) and build_cleaner_message (cleaner.py:183) now excludes bool via `is_bool_dtype`, so bool columns take the distinct-values branch. A 990 False / 10 True column now samples `['False', 'True']`; unique_count was already correct and is unchanged. Covered by test_profiler.py Group 9 and test_cleaner.py Group 6. See decisions.md 2026-09-19.

**2026-09-17 | Cleaner raises TypeError on any bool-dtype (True/False) column — pre-existing, FIXED in 4961e9f (Build A)**
Found while building the Profiler row-sampling fix and reproduced locally without API calls. pd.read_csv parses a column of True/False values as bool dtype.
Root cause: pandas' is_numeric_dtype returns True for bool, but bool.quantile() raises "TypeError: numpy boolean subtract, the `-` operator, is not supported". cleaner.py filters numeric columns with is_numeric_dtype and then calls quantile() in three places: detect_interactions (fails with 4+ non-null values; cleaner_node calls it before the Cleaner's LLM call), build_cleaner_message's IQR block (5+ values), and execute_cleaning_operations' OUTLIER FLAG branch. Any upload containing such a column therefore fails the pipeline at the Cleaner with SYSTEM_ERROR, after the Profiler call has already been billed. The Profiler does not have this problem: compute_column_stats excludes bool explicitly.
Solution: not yet applied (out of scope for the row-sampling fix; needs approval). Likely fix: add `and not pd.api.types.is_bool_dtype(...)` to those three numeric filters, matching compute_column_stats, plus a test_cleaner.py regression test with a bool column.
Status update (2026-09-19, Build A): FIXED, committed in 4961e9f. The three numeric filters (cleaner.py:197 IQR block, :266 detect_interactions, :423 OUTLIER FLAG) now add `and not pd.api.types.is_bool_dtype(...)`, inline, with no helper. Re-verified before the fix: crashes at 4+ non-null values in detect_interactions and 5+ in the other two. test_cleaner.py Groups 6-7 cover each site, plus a mocked cleaner_node run on a bool column that reaches the LLM call and saves status=cleaned. Flagging outliers on a bool column is now a no-op, an accepted behavior change. Full suite 139 passed / 16 skipped. No live run is needed, because both the failure and the fix happen before the Cleaner's LLM call. See decisions.md 2026-09-19.

**2026-09-17 | build_profiler_message raises TypeError on Excel files whose header row contains dates — pre-existing, FIXED in 892ae98 (Build C)**
Found while planning the Profiler overwrite guarantee; reproduced locally without API calls using a scratch .xlsx with header row `region | 2024-01-01 | 2024-02-01`. pd.read_excel keeps date header cells as `datetime.datetime` column labels.
Root cause: `column_info` and `computed_column_stats` are dicts keyed by column name, and json.dumps only accepts str/int/float/bool/None as keys. `default=str` converts values, not keys, so build_profiler_message raises "TypeError: keys must be str, int, float, bool or None, not datetime.datetime". profiler_node's except block turns this into SYSTEM_ERROR before the Profiler's LLM call is made (no API cost), so any such upload fails at the Profiler.
Solution: not yet applied (needs approval). Likely fix: convert column names to str before the message dicts are built, keeping the names consistent with what later agents see.
Status update (2026-09-20, Build C): FIXED, committed in 892ae98. Both upload loaders — profiler.load_dataframe and cleaner.load_dataframe_from_uploads — now end with `df.columns = df.columns.map(str)`, so no agent ever sees a non-string column label. Three further problems were found and fixed by the same line: build_cleaner_message crashes identically and independently (the Profiler-only fix would have moved the crash downstream, past a billed LLM call); integer headers do not crash at all but make execute_cleaning_operations silently skip every cleaning decision, because json.dumps emits int keys as JSON strings and the DataFrame labels stay ints (verified: a NaN survives a "fill missing with mean" decision); and map(str) is required over astype(str) so that upload-side names match the parquet-side names the Analyzer reads (astype(str) truncates a DatetimeIndex to "2024-01-01" where parquet writes "2024-01-01 00:00:00"). 16 new tests; full suite 192 passed / 16 skipped; all 5 deliberate breaks caught. No live run needed — every failure and every fix is before or independent of the LLM calls. See decisions.md 2026-09-20.
Correction (2026-09-20, Build C): the evaluation preceding this build reported that uniform numeric or date headers also crash the Analyzer in `_is_id_column`, and treated that as its most significant finding. It does not reproduce. `to_parquet`/`read_parquet` returns string column labels for every index dtype tested (int64 Index, object Index of ints, RangeIndex, DatetimeIndex, mixed str/datetime) on the pinned pandas 2.2.0 / pyarrow 23.0.1, and the Analyzer's only data source is that parquet. The Build B claim in decisions.md 2026-09-19 that "pyarrow turns non-string column names into strings on the round trip" is therefore correct as written and was NOT corrected. `_is_id_column` does raise TypeError on a raw int or datetime label, so tests/test_analyzer.py Group 11 pins pyarrow's stringification as the thing that prevents it.

**2026-09-17 | LangSmith network calls at import time and on every create_tracer() call — observation, not fixed**
Observed while planning the Profiler node-level test; reproduced locally with sockets blocked, so no traffic left the machine.
(1) Importing backend/utils/langsmith_client.py runs validate_langsmith_connection(), a real GET /sessions call. Offline it RAISES RuntimeError ("LangSmith connection failed — LangSmithConnectionError ...") after about 3.5s. That fail-fast behaviour is by design, but it means profiler.py — and every test module that imports an agent — cannot be imported without network access to api.smith.langchain.com. The langsmith Client it creates also starts a background thread that calls GET /info; offline, that thread only logs a WARNING.
(2) Every create_tracer() call builds a LangChainTracer whose client starts a background tracing thread that calls GET /info. Offline, create_tracer() still returns normally, and the failure shows up only as a "langsmith.client: Failed to get info" WARNING. profiler_node calls create_tracer("profiler") on every run, so node-level unit tests should mock it (the new test_profiler.py Group 8 test does).
Correction to the planning notes, which said "neither raises offline, both logged": the local reproduction shows that is true only for (2). (1) raises.
Solution: none applied — observation only. If tests ever need to run offline, the import-time validation would have to move out of module import (for example, into app startup); not approved.

**2026-09-17 | security-guidance plugin logs a failed or rate-limited (HTTP 429) LLM review as "no vulnerabilities found" — observation, not a project bug**
Observed while inspecting the security-guidance Claude Code plugin. Its end-of-turn LLM code review runs on Claude Code's own login, not the project's API key. During the 2026-09-16 push review it hit HTTP 429 three times, and the log entries right after those errors read "LLM code review: no vulnerabilities found".
Root cause: the plugin's hooks/llm.py (marketplace copy, around line 1046) logs "LLM code review: no vulnerabilities found" whenever the analysis result is empty — `if not analysis or not analysis.get("hasVulnerabilities") ...` — and a failed or rate-limited call also produces an empty result. In the log, a review that never ran looks the same as a real clean pass.
Solution: none — this is plugin behaviour outside the repo. Do not treat a "no vulnerabilities found" line as proof a review ran; check for 429 errors around it, and run the Security Review plugin explicitly before any deployment (CLAUDE.md rule 14).

**2026-09-18 | Excel nrows=1 peek reports 0 data rows for workbooks whose second row is blank — found and fixed during the Build D build**
Found while building the empty-upload check (Build D) and reproduced in tests/test_file_handler.py before the fix: `validate_file` rejected valid .xlsx files, including the common "title row, blank row, table" layout, with "USER_ERROR: This file has no data rows."
Root cause: pandas 2.2.0's openpyxl reader reads only header + nrows physical rows and then trims trailing blank rows (`get_sheet_data`, pandas/io/excel/_openpyxl.py), while the full read keeps interior blank rows (`skip_blank_lines=False`, _base.py). With nrows=1 a blank second row is trimmed, so the peek comes back empty although the full read has data. `read_csv` is not affected, because it skips blank lines before counting nrows.
Solution: in `_has_no_data_rows`, an empty Excel peek is confirmed with a full `pd.read_excel` (the Profiler's own read) before rejecting. Covered by `test_accepts_xlsx_with_blank_second_row` (2 layouts). See decisions.md 2026-09-18.

**2026-09-18 | Every exception inside an agent node is reported as SYSTEM_ERROR, including user-fixable ones — pre-existing, NOT yet fixed (deferred)**
profiler.py:313, cleaner.py:621, analyzer.py:818 and explainer.py:209 (and orchestrator.py:255) all build `error_message` as `f"SYSTEM_ERROR: {str(exc)}"` for any exception. A problem the user could fix therefore reaches the UI as a system failure; before Build D, a 0-byte CSV sent straight to the API failed in the Profiler with pandas' "No columns to parse from file" as SYSTEM_ERROR. Build D moves empty and header-only files out of the pipeline but does not change this.
Solution: not applied (deferred). A proper fix means auditing the exception types each of the four nodes can raise and deciding which are user-fixable; that is separate scope.

**2026-09-18 | Cleaner can reduce a valid file to 0 rows mid-pipeline; the Analyzer completes but reports a 1.0 data-quality score — pre-existing, NOT yet fixed (deferred)**
From the Build D investigation pass: when the Cleaner's own operations leave 0 rows, the Analyzer does not crash but still reports data_quality_score 1.0. Traced 2026-09-18: `compute_data_quality_score` (analyzer.py:396) starts at 1.0 and deducts only for columns with missing values, duplicate rows, and outlier/flag cleaning decisions. A 0-row frame has no missing values or duplicates, so it scores 1.0 (0.9 if a cleaning decision mentions outliers or flagging). The function does not read the Cleaner's missingness patterns, so this is independent of the cleaner.py:61 NaN bug in the next entry. Build D only rejects files that arrive with zero data rows; it does not cover files the Cleaner empties.
Solution: not applied (deferred). Likely fix: when the cleaned frame has 0 rows, return a low score or flag the result instead of scoring it as clean.

**2026-09-18 | cleaner.py:61 missingness guard never fires on a 0-row frame (NaN == 0 is False) — pre-existing, NOT yet fixed**
`analyze_missingness_patterns` computes `missing_pct = missing_mask.mean() * 100`. On a 0-row frame the mean is NaN and `NaN == 0` is False, so the "skip columns with no missing values" guard at line 61 never fires. Reproduced locally: a header-only frame returns every column as `classification: random` with `missing_pct: nan`, a fabricated finding. Its only caller (cleaner.py:481) runs on the raw uploaded file, and after Build D no CSV/XLSX upload reaches it with 0 rows; the comparison itself is unchanged.
Solution: not applied. Likely fix: test `missing_mask.sum() == 0` (or return early when `len(df) == 0`) instead of comparing the mean.

**2026-09-18 | .xls uploads always fail in the Profiler because xlrd is not installed and not in requirements.txt — pre-existing, NOT yet fixed**
Found during Build D verification. FileUpload.tsx:27 lists ".xls" in VALID_EXTENSIONS and file_handler's `_ALLOWED_EXTENSIONS` accepts it, but `pd.read_excel` on an .xls raises "ImportError: Missing optional dependency 'xlrd'". profiler.load_dataframe therefore fails on every .xls upload and profiler_node reports it as a confusing SYSTEM_ERROR. Build D does not change this: its peek hits the same ImportError and passes the file through (fail-open).
Solution: not applied (needs approval). Either add xlrd to requirements.txt, stating the reason (CLAUDE.md rule 3), or stop advertising .xls.

**2026-09-18 | Corrupted files and non-UTF-8 CSVs still reach the pipeline and fail there as SYSTEM_ERROR — Definition of Done item 4 still open**
Build D's data-row peek is deliberately fail-open. Anything pandas cannot read (UnicodeDecodeError on e.g. a Latin-1 CSV, ParserError, an .xlsx that is not a valid zip) is logged and passed through, and the Profiler's read then fails with SYSTEM_ERROR, as it did before Build D.
Solution: not applied. Detecting these at upload needs its own design (user-facing message, whether to try other encodings); separate future item.

**2026-09-18 | A header plus one entirely empty data row passes the empty-file check — known edge case, not addressed**
`a,b\n,\n` parses as 1 row of NaN, so `_has_no_data_rows` returns False and the file enters the pipeline with a single all-missing row.
Solution: not applied; narrow edge case, out of scope for Build D.

**2026-09-18 | Upload rejections render as red "api" errors with the raw "USER_ERROR:" prefix — observation, FIXED in 2410083 (Build E)**
FileUpload.tsx:206-209 turns any failed upload into `{ type: "api", message: err.message }`, and `err.message` is the backend's `detail` verbatim (frontend/lib/api.ts `toError`). The error box uses amber styling only for `type === "user"`, so a backend USER_ERROR 400 shows in red, prefix included. The frontend pre-checks type, 0 bytes and size (FileUpload.tsx:113-133), so until Build D no backend 400 was reachable from the UI; header-only files are the first. AnalysisProgress.tsx:419 already classifies errors with `startsWith("USER_ERROR:")`.
Solution: not applied (frontend is outside Build D). Likely fix: in FileUpload's catch, map a `USER_ERROR:` detail to `type: "user"` and strip the prefix.
Status update (2026-09-25, documentation cleanup): FIXED in Build E, committed in 2410083 (2026-09-21). `classifyUploadError()` in FileUpload.tsx strips the `USER_ERROR:`/`SYSTEM_ERROR:` prefix and picks amber (user) or red (api) styling, with a generic fallback for unprefixed failures such as axios "Network Error"; live-verified at 320px against a real backend 400 (header-only CSV). See decisions.md 2026-09-21 (Build E upload errors).

**2026-09-19 | Excel True/False columns with blank cells load as float64 (1.0/NaN/0.0), not bool or object — observation, NOT yet fixed (recorded during Build A as a named exception to its "no new errors.md entry" scope)**
Unlike CSV, where blanks make the column object dtype, pd.read_excel turns True/blank/False into float64 `[1.0, NaN, 0.0]` (reproduced on pandas 2.2.0 with an openpyxl-written .xlsx). The column counts as numeric, not bool, so Build A's `is_bool_dtype` exclusion does not reach it. It does not crash, but it takes the random-sample branch (a misleading homogeneous sample on imbalanced data) and gets IQR/mean stats, as if it were a continuous measure. Solution: not applied; needs its own design (for example, detecting 0/1-only float columns loaded from Excel).

**2026-09-19 | generate_correlation_heatmap selects numeric columns by dtype on its own, ignoring the Analyzer's numeric_columns list — pre-existing, NOT yet fixed (recorded during Build B as a named exception to its analyzer.py-only scope)**
`generate_all_charts` (viz_tools.py) builds histograms, box plots and the scatter plot from the `numeric_columns` list it receives from classify_columns. `generate_correlation_heatmap` (viz_tools.py:103) instead calls `df.select_dtypes(include="number")` on the whole cleaned DataFrame. As a result, a genuine numeric ID column that the Cleaner failed to convert to string (for example `customer_id` still int64) is excluded from the correlation matrix sent to the LLM but still appears in the heatmap. That remains true after Build B.
Positive side effect of Build B: it resolves the most visible symptom of this mismatch. Before the fix, the iris heatmap already showed all 4 measurements while the correlation matrix the LLM reasoned over covered only sepal_length and petal_length, so the chart and the analysis disagreed. Now both show all 4 columns and agree on every fixture except when an unconverted genuine ID column is present.
Solution: not applied (viz_tools.py is out of Build B's scope). Likely fix: pass `numeric_columns` into generate_correlation_heatmap and select `df[numeric_columns]`, matching compute_correlation_matrix.

**2026-09-20 | Excel header cells that are themselves a literal boolean still silently skip every cleaning decision for that column — residual of Build C, NOT yet fixed**
Found while verifying Build C's commit message (892ae98) rather than assumed from it; reproduced locally without API calls. This is the boolean case of the same bug family as Build C's problem 3 (see decisions.md 2026-09-20). Build C's fix did remove the crash class for these headers — the label is now a string and nothing raises — but the decision-matching gap survives it.
Root cause: the two sides stringify a bool differently. json.dumps renders a Python `True` dict key as lowercase `"true"`, per the JSON spec, so that is the name the Cleaner's LLM sees and the name it puts in `column_name`. The loaders' `df.columns.map(str)` renders the same label as Python's `"True"`. execute_cleaning_operations (cleaner.py:340) then hits `col not in df.columns`, logs "Skipped decision — column 'true' not found in dataframe", and applies nothing — while the cleaning_report still claims the action was made. Measured: raw label `True` → LLM sees `'true'`, actual label `'True'`, no match. Integer and decimal headers do NOT have this problem (`2021` → `"2021"` on both sides; `2021.5` → `"2021.5"` on both sides), which is why Build C's fix closes those completely.
Confirmed rare: this needs the header CELL itself to hold a real boolean — a sheet whose first row contains TRUE/FALSE as boolean values rather than text. A column merely containing boolean DATA is unaffected and is a different, already-handled case (see the 2026-09-17 Cleaner bool-dtype entry and the 2026-09-19 Excel float64 True/False entry).
Solution: not applied — logged as a backlog item rather than folded into Build C, which was already committed. Likely fix: normalize on the JSON side as well, so both sides agree on one spelling (for example map a bool label with `json.dumps(label)`-compatible casing before `map(str)`), or match decisions case-insensitively in execute_cleaning_operations. Either choice needs its own decision entry — the first changes column names users see, the second loosens matching for every column, not just bool-labelled ones.

**2026-09-21 | generate_line_chart has the same long-title clipping as the scatter plot — found during Build E, NOT fixed (out of scope)**
Build E fixed the scatter-plot title (`generate_scatter_plot`, viz_tools.py) with `_short_name(name, limit=26)` plus the `<br>` / font-size-14 / `automargin=True` / `yref="container"` recipe. `generate_line_chart` (viz_tools.py:149) builds its title the same untreated way — `title=f"{value_column} Over Time"` passed straight to `px.line` — so a long `value_column` clips or crowds exactly as the scatter title did. Deliberately left alone: it is untested (no test covered it before this build), it was not part of the approved Build E scope, and changing it would ship an unverified edit alongside a verified one. Note on naming: the Build E order referred to this function as `generate_time_series_chart`; no such function exists — the real name is `generate_line_chart`, and it is the function meant. Fix when the time-series chart gets its own tested build: apply `_short_name` and the same `update_layout(title=...)` recipe, and cover it with the equivalent of tests/test_viz_tools.py's adversarial long-name test.

**2026-09-21 | A shared or reloaded analysis link is unrecoverable because session_id lives only in the uploader's localStorage — pre-existing, FIXED in 25c8725**
HIGH VISIBILITY — this is a user-facing dead end, not a cosmetic issue. `FileUpload.handleUpload` writes `session_id_{analysis_id}` to localStorage on the uploading browser only (FileUpload.tsx; see decisions.md 2026-05-17), and `GET /api/analysis/{id}` requires that value in the `session-id` header — without it the API returns `{"detail":"Invalid or missing session-id header."}` (confirmed directly this session against a real completed analysis). Consequences: an /analysis/{id} link sent to a colleague, opened on another device, opened in a private window, or opened after the user clears site data can never load its results, even though the analysis completed and its data is intact in Supabase. There is no recovery path in the UI — no "enter your session id", no re-auth, no error state that explains what happened. Explicitly excluded from Build E's scope. Not a quick fix: the real question is whether session_id should become a bearer token embedded in the shareable URL (recoverable, but anyone holding the link gets the data), stay per-browser (private, but unshareable), or be replaced by real per-user auth. That tradeoff needs its own dedicated evaluation round before any code is written.
Status update (2026-09-21, committed in 25c8725): FIXED, by making read-only access public by analysis_id rather than by embedding session_id in the URL or adding auth (reasoning in decisions.md 2026-09-21). The four GET routes (`/api/analysis/{id}`, `/status`, `/question/{question_id}`, `/charts`) now depend on the new `get_public_read_access` instead of `get_session`; POST `/question` and POST `/resume` still depend on the unchanged `get_session` (`git diff -U0 backend/main.py` shows no hunk inside it). The frontend no longer gates rendering on localStorage: a visitor — including the real owner on a different device, in a private window, or after clearing site data — now sees the full three-layer results, or the live read-only progress view, and QuestionInput renders disabled with an explanation. The "Session not found. Please upload your file again." block is removed. Verified with fresh browser contexts (no localStorage) against 1a96db4a (complete) and 874a305b (error), stubbed running and paused states, a nonexistent UUID and a malformed id, plus an owner regression run — all at 320px (overflow 0) and 1280px; zero POST requests were made by any visitor run.

**2026-09-21 | AnalysisProgress.tsx still uses text-xs at 3 sites — known, accepted, temporary inconsistency**
Build E raised the mobile minimum font size from 12px to 14px across 21 sites in AnalysisResults.tsx, InsightReport.tsx, FileUpload.tsx, QuestionInput.tsx and CodeBlock.tsx. AnalysisProgress.tsx:227, :296 and :354 were deliberately left at `text-xs`, so the progress screen renders 12px text where every other screen now renders 14px. Accepted for now and scoped out by the Build E order: those three sites sit in the progress/pause region that the deferred pause-state UI build will rework, and changing them twice is wasted churn. This is a known inconsistency with `docs/ui-and-frontend.md`'s 14px mobile minimum, not an oversight. Resolve it as part of the pause-state UI build. The two shadcn primitives ui/badge.tsx and ui/button.tsx also retain `text-xs` by design — they are shared primitives whose variants are set by the design system, and Build E deliberately did not touch them.

**2026-09-21 | Upload size-limit copy hardcodes "100MB" in three places that can drift from MAX_FILE_SIZE — found during Build E, tracked not fixed (low priority)**
The 100MB limit is enforced from two independent constants — `MAX_FILE_SIZE_BYTES = 100 * 1024 * 1024` (FileUpload.tsx) and `MAX_FILE_SIZE` (backend/config.py, checked in file_handler.validate_file) — but the user-facing copy that states the number is a plain string literal in three places: FileUpload.tsx's oversized message ("This file is too large. Maximum size is 100MB."), file_handler.py:33's identical USER_ERROR message, and docs/ui-and-frontend.md:121 where that wording is specified. Raising or lowering either constant therefore silently leaves all three strings lying to the user, and no test would catch it — the string assertions in tests/test_file_handler.py and tests/test_api.py assert the literal copy, not agreement with the constant.
Build E introduced the frontend literal deliberately rather than deriving it: `formatBytes(MAX_FILE_SIZE_BYTES)` renders "100.0 MB", which is not the spec wording ("100MB"), and the backend already hardcoded its own copy the same way, so the literal matched the established convention and kept frontend and backend messages byte-identical. That tradeoff is recorded here rather than hidden.
Not fixed now because the real fix crosses the frontend/backend boundary: a single shared source of truth means either exporting the limit from the API (e.g. a config endpoint or a build-time constant generated from backend/config.py) or accepting duplication and adding a test that asserts the rendered copy matches the constant on each side. That is its own scope. Cheap interim guard if it comes up sooner: a unit test per side asserting the message contains the value formatted from its own constant.

**2026-09-21 | errorRef can be nulled by an exiting AnimatePresence banner, silently skipping the scroll-into-view — found by code review during Build E, NOT fixed**
`FileUpload.tsx`'s error banner is keyed `error-${error.type}` inside `AnimatePresence` with a 200ms exit animation, and both the entering and exiting nodes share the same `errorRef` object. When `error.type` flips (user → api), both are mounted briefly; when the old one unmounts React runs its ref cleanup and sets `errorRef.current = null`, clobbering the pointer to the banner that is still on screen. The scroll for that error still happens (the `[error]` effect runs at commit, well before the 200ms unmount), so the visible symptom is deferred: a *subsequent* error of the same type re-uses the same key, so the node is not remounted, the ref is never re-attached, and `errorRef.current?.scrollIntoView(...)` no-ops. Reachable path: select a valid CSV (preview mounts), then select a `.txt` (user error; `handleFileSelection` returns early and leaves the old file selected), then Start Analysis with a failing backend — if the request settles inside the 200ms exit window the two banners overlap. Impact is cosmetic (a missed scroll, no wrong data, no crash), which is why it is tracked rather than fixed mid-build. Assessed by reasoning about React ref-detach semantics; NOT reproduced in a browser. Fix: use a callback ref that ignores the detach (`node => { if (node) errorRef.current = node; }`), or attach the ref to a stable wrapper outside `AnimatePresence`.

**2026-09-21 | An upload whose multipart part carries no filename returns 500 instead of a USER_ERROR 400 — found by code review during Build E, pre-existing, NOT fixed**
Starlette sets `UploadFile.filename` to `None` when the multipart part has no `filename=` parameter. `main.py:147` passes it straight into `validate_file`, whose first act is `Path(filename).suffix` — `Path(None)` raises `TypeError`, which the endpoint's `except ValueError` does not catch, so the request becomes an unhandled 500 rather than the intended `USER_ERROR: Please upload a CSV or Excel file (.csv, .xls, .xlsx)` 400. Not reachable through the project's own UI (the file input always supplies a filename), but reachable by any hand-built or malformed multipart request. Pre-existing; main.py was not touched by Build E. Fix: pass `file.filename or ""` at the call site, which routes it into the existing extension check and the correct 400.

**2026-09-21 | The "approx. N columns" upload preview is computed for Excel files too, where it is meaningless — found by code review during Build E, pre-existing, NOT fixed**
`FileUpload.tsx`'s preview effect slices the first 2048 bytes of any accepted file and computes `firstLine.split(",").length` regardless of extension. For `.xlsx`/`.xls` — zip containers, not text — this counts commas in compressed binary and renders a confident but arbitrary number ("approx. 7 columns") next to the file size. Cosmetic and pre-existing; Build E changed only the font size of the line that displays it. Fix: gate the effect on `getExtension(file.name) === ".csv"` and render the column count only when it was actually derived, leaving the size alone for Excel.

**2026-09-21 | Pre-Build-B analyses show a 2×2 interactive correlation matrix beside a 4×4 PNG heatmap — cosmetic, accepted, old data only**
On analysis 1a96db4a-3c88-460d-a39a-820b619536a4 (iris, 2026-09-18, before Build B), the stored `analysis_report.correlation_matrix` covers only `petal_length` and `sepal_length`, while the PNG heatmap on disk shows all four measurements, including `sepal_width` and `petal_width`. Root cause (by reasoning, consistent with decisions.md's Build B entry): before Build B, `classify_columns` dropped every numeric column whose name contained "id", so "w**id**th" columns were excluded from the Analyzer's matrix, while the heatmap PNG was generated from its own numeric-column selection. The new interactive matrix faithfully renders the stored 2×2, so on this one analysis the two views disagree. Post-Build-B analyses are unaffected because the same filter now feeds both paths. Solution: none — accepted. It affects stored pre-fix data only, it is cosmetic, and regenerating old reports would require a pipeline run. Related but distinct: the open viz_tools task to pass `numeric_columns` into `generate_correlation_heatmap` (tasks.md, errors.md 2026-09-19).

**2026-09-21 | Analyzer states "n=15 ... above the 30-pair reliability floor" — self-contradictory statistical claim, and it downgraded the confidence label it drives — intelligence-quality issue, NOT fixed**
Found while assessing the (deferred) causal-reasoning hover. Stored Analyzer output describes n=15 complete pairs as "above the 30-pair reliability floor". 15 is below 30. The brief attributed this to analysis 7e0797c9, but a query across every stored report shows otherwise: 7e0797c9 states it correctly ("n=15 rows, which is below the 30-pair reliability floor"). The contradictory wording appears in three other analyses: 1a96db4a-3c88-460d-a39a-820b619536a4 (Analyzer `confidence_reasoning`: "n=15 complete pairs, which is above the 30-pair reliability floor but only barely", and propagated by the Explainer into `insight_report`: "n=15 is above the 30-pair reliability floor only marginally"), 9e5c7266-b31d-4183-a067-99977375fc17 ("above the 30-pair reliability floor but only marginally so", "above the 30-pair computation floor"), and 01f110f4-423a-4a25-bcb5-40822a51441e (sepal_length × sepal_width, r=0.74, n=15; its correlation summary says n=15 "clears the 30-pair minimum threshold for computation but not for High confidence", and its `confidence_reasoning` says "The sample clears the 30-pair floor for computation but is less than half that floor", which contradicts itself within one sentence). This is not only wording. `backend/prompts/analyzer_system.md` §6 (line 195) and Step 5 (line 269) require confidence **Cannot Determine** for any correlation with fewer than 30 complete pairs. All three analyses instead store **Low** in `analysis_report.correlation.strong_correlations[0].confidence_level`, read directly from the field via Supabase on 2026-09-21: 1a96db4a (r=0.885), 9e5c7266 (r=0.885; this supersedes the earlier narrative-only evidence) and 01f110f4 (r=0.74). That is three of three affected analyses mislabeled, and zero landed on the correct label. The misreading of the floor therefore produced a miscalibrated confidence label, which violates the Calibrated Confidence Framework and non-negotiable rule 11. The Explainer behaved differently across the three. On 1a96db4a it repeated the false claim ("n=15 is above the 30-pair reliability floor only marginally"). On 01f110f4 its `insight_report` stated the fact correctly ("n=15 is below the 30-pair minimum for reliable Pearson correlation estimation") but did not flag or correct the Low label. So even when the Explainer knows the fact, it does not check it against the label. Probable cause (not verified): the prompt describes the floor in prose, and the model sometimes frames it as a minimum for *computing* r rather than a ceiling on *confidence*. Nothing in Python enforces the floor after the LLM responds. Suggested fix, its own scope: enforce the floor in Python after the Analyzer response. For every strong pair with complete-pair n < 30, force `confidence_level = "Cannot Determine"` in code, like `apply_computed_column_stats` overwrites copied column stats, and add an Explainer self-check for numeric comparisons against the stated floors. Unrelated to the correlation-matrix frontend build; tracked separately.

**2026-09-21 | shadcn color utilities (`text-muted-foreground`, `bg-card`, `border-border`, ...) compile to no CSS at all — found during the correlation-matrix build, pre-existing, FIXED in d2715be**
The frontend runs Tailwind v3 (`tailwindcss ^3.4.1`, `@tailwind base/components/utilities` in globals.css), but `tailwind.config.ts` maps only `background` and `foreground` into `theme.extend.colors`. The shadcn tokens (`--muted-foreground`, `--card`, `--primary`, `--border`, ...) are defined as CSS variables, but the Tailwind classes that reference them have no theme entry, so v3 silently emits nothing for them. `@import "shadcn/tailwind.css"` does not fill the gap: that file is Tailwind v4 `@theme inline` syntax, which v3 does not process. Verified in the production build: the compiled CSS contains zero rules for `.text-muted-foreground` or `.bg-card`, and the only color utilities present are `bg-background`/`text-foreground` plus explicit palette classes (`text-red-200`, `bg-amber-500/10`, ...). Visible effect across the app: every "muted" label renders in full foreground color, card surfaces are transparent, and `/40`, `/60` opacity variants do nothing. `border-border` looks right only because globals.css sets `* { border-color: var(--border) }`. This has been true since the scaffold, and earlier visual checks (including Build E's) did not catch it because the page still looks coherent in forced dark mode. The correlation-matrix component works around it locally with `var()` arbitrary classes (`text-[var(--muted-foreground)]`, `bg-[var(--muted)]`, `outline-[var(--ring)]`). Fix, its own scope: add the shadcn color map to `tailwind.config.ts` (`muted: { DEFAULT: "var(--muted)", foreground: "var(--muted-foreground)" }`, etc.; opacity modifiers on `var()`-only colors also need the `<alpha-value>`/color-mix approach), then re-screenshot every page, because many surfaces will change appearance at once.
Status update (2026-09-21, committed in d2715be): FIXED. Root cause confirmed: every token in globals.css is a complete `oklch()` color, so the channels + `<alpha-value>` pattern in v3's docs can't apply. `tailwind.config.ts` mapped only `background`/`foreground`, and those as bare `var()`, so the other 32 tokens produced no utilities and opacity variants (`text-foreground/90`) were dropped. All 34 color tokens are now mapped through `color-mix(in oklch, var(--token) calc(<alpha-value> * 100%), transparent)`. v3 substitutes `<alpha-value>` as a plain string (pluginUtils.js), and this is the same output v4 generates. Also added `darkMode: "selector"`: without it the newly emitted `dark:` variants would have followed the OS setting instead of the forced `.dark` class. No change to globals.css or any component. CorrelationMatrix's `var()` arbitrary classes and inline `color-mix()` fills are unaffected (fills measured identical before and after). Intended visible changes: muted text dims, cards gain a surface, `/60` borders go from 10% to 6% alpha in dark mode, and the Limitations callout's primary left border now renders. Still open and logged separately: the dead v4-only `@import`s.
Verified (computed styles, identical probe before and after the config change, at 320px and 1280px on the upload page, 1a96db4a and 7e0797c9, collapsed and fully expanded). The first "after" capture was invalid and was discarded: the running `next dev` did not pick up the `tailwind.config.ts` change by hot reload, so it still served the old CSS (zero `text-muted-foreground` rules) and every measurement came back byte-identical to "before". The dev server was restarted, the served CSS was confirmed to contain the new rules, and the full capture was redone; every figure below is from that second, valid run. Results: `text-muted-foreground` went from `oklch(0.985 0 0)` to `oklch(0.708 0 0)`; `bg-card/40` from transparent to `oklch(0.205 0 0 / 0.4)`; `border-border/60` from `oklch(1 0 0 / 0.1)` to `oklch(1 0 0 / 0.06)`; the Limitations callout's left border from `oklch(1 0 0 / 0.1)` (identical to its top border) to `oklch(0.922 0 0 / 0.5)` against a 6% top border. Every CorrelationMatrix cell fill and the legend gradient were string-identical before and after (hue 45 on positive cells, `var(--muted)` `oklch(0.269 0 0)` on null cells, legend stops at hue 250/45); neither stored analysis has a negative cell, so hue 250 is covered by the legend stops only. Page overflow at 320px stayed 0 everywhere. Additional visible change not listed above: the upload page's context textarea was stark white before (its `dark:bg-input/30` emitted nothing) and is now the dark input surface. Production build CSS: `.text-muted-foreground`, `.bg-card\/40`, `.border-border\/60`, `.border-l-primary\/50`, `.text-foreground\/90` all present; 8 `:where(.dark,.dark *)` rules; zero `prefers-color-scheme` rules. Lesson for future config changes: restart `next dev` after editing `tailwind.config.ts` before capturing anything.

**2026-09-21 | globals.css imports two Tailwind v4-only files that v3 passes through as dead CSS; v4-only variants in components/ui are no-ops; body font is not DM Sans — pre-existing, body-font part FIXED in 9d71407; the rest NOT fixed**
`@import "shadcn/tailwind.css"` and `@import "tw-animate-css"` contain `@theme inline`, `@custom-variant` and `@utility` blocks. Tailwind v3.4 does not process them, and the production CSS ships them raw, where the browser ignores them. As a result, `data-open:`/`data-closed:` accordion animations, `animate-in`/`fade-in-*`, `no-scrollbar`, `has-data-[…]:`, `not-last:` and `aria-invalid:` (in `components/ui/*`) emit nothing. `.theme { --font-sans: var(--font-sans) }` refers to itself and is unused. `font-sans` resolves to v3's default stack because no `fontFamily` maps `--font-sans`, so body text likely renders in the system font rather than DM Sans. Found during the color-token evaluation, which fixed colors only.
Status update (2026-09-22, font part only; committed in 9d71407): **the body-font part is FIXED; the rest of this entry stays open.** Confirmed rather than "likely": computed `font-family` on `body` and `html` was Tailwind v3's default stack (`ui-sans-serif, system-ui, …`) on the upload page and a completed results page (1a96db4a), at 320px and 1280px, collapsed and fully expanded. That was 16 text elements on the upload page, and 38 collapsed / 75 expanded on the results page. The only DM Sans text was the upload subtitle, which sets `var(--font-sans)` inline (app/page.tsx). Root cause: the same mechanism as the color-token bug above. next/font defines `--font-sans` correctly on `<html>`, but `tailwind.config.ts` never mapped it, so `font-sans`, the globals.css `html { @apply font-sans }` rule and Preflight's `html,:host` rule all compiled to the default stack. Fix: `theme.extend.fontFamily.sans = ["var(--font-sans)", ...defaultTheme.fontFamily.sans]`. The generic stack is appended because next/font's variable holds only `'__DM_Sans_<hash>', '__DM_Sans_Fallback_<hash>'` and no generic family. **Instrument Serif was never affected.** Every heading's visible text sets `var(--font-display)` by inline style (14 sites), which never goes through Tailwind, and its computed values were byte-identical before and after. The three accordion-header `<h3>` wrappers, which Radix renders around the triggers, compute to the body font but contain no text of their own; their visible titles are Instrument Serif via `TriggerLabel`'s inline style. Still open in this entry: the two dead Tailwind v4 `@import`s, the no-op v4-only variants in components/ui, the self-referencing `.theme { --font-sans: var(--font-sans) }` block (unused: no element has the class), and `font-heading` in ui/card.tsx (emits nothing; `CardTitle` is not rendered anywhere). Details in decisions.md 2026-09-22.

**2026-09-21 | RLS is disabled on `analyses` and `questions`, and the `anon` role holds full SELECT/INSERT/UPDATE/DELETE on both — pre-existing, NOT fixed, recommend prioritizing**
Checked directly via the Supabase MCP during the shared-link evaluation: `relrowsecurity = false` on both tables, zero policies, and `has_table_privilege('anon', …)` true for SELECT, INSERT, UPDATE and DELETE. Anyone holding the project URL and the publishable (anon) key can read, modify or delete every row through the Data API, including every stored `session_id` — so the `get_session` check on POST question/resume is only as strong as the secrecy of a key that is designed to be public. Current exposure is nil in practice: the frontend never talks to Supabase (zero references in app/, components/, lib/, package.json, next.config.mjs; `.env.local` holds only `NEXT_PUBLIC_API_URL`), and the key's value appears in no git commit and not in the built `.next/static` bundle. The backend uses only `SUPABASE_SECRET_KEY` (backend/utils/supabase_client.py); `SUPABASE_PUBLISHABLE_KEY` is loaded and required by backend/config.py:10/21 but used nowhere. The shared-link fix does not change this risk: the newly public GET routes return what a direct anon read already could. Recommendation: move the existing "Supabase RLS policies" task ahead of other infrastructure work. Enabling RLS with zero policies on both tables is a one-line migration per table that blocks `anon` entirely, while the backend's secret (service role) key bypasses RLS, so no application code changes. Separately consider dropping `SUPABASE_PUBLISHABLE_KEY` from config.py's required list, since nothing uses it. The cleaned-datasets storage bucket's access policies were not investigated.

**2026-09-21 | A malformed analysis_id or question_id returned HTTP 500 instead of 404 — found during the shared-link evaluation, FIXED in the same build (committed in 25c8725)**
`client.table("analyses").select(...).eq("id", "not-a-uuid")` raises postgrest `APIError` 22P02 ("invalid input syntax for type uuid"), which main.py did not handle, so any request with a non-UUID id — a truncated or mistyped shared link — returned 500 (confirmed against the real database; a well-formed but nonexistent UUID correctly returned 0 rows and a 404). On the frontend, AnalysisProgress turned every failed status fetch into a silent retry, so a 500 or 404 showed "Loading analysis…" forever while polling every 3 s. Fix: `get_public_read_access` requires `analysis_id` to be a canonical hyphenated UUID and `get_question` checks `question_id` the same way (shared helper `_is_canonical_uuid`), both returning 404 before any database call (tests assert `get_supabase_client` is never called). The first version used a bare `uuid.UUID()` parse; Code Review caught that it also accepts a `urn:uuid:` prefix, which Postgres still rejects with 22P02 (confirmed live: 500), so the check now also requires `str(uuid.UUID(x)) == x.lower()` — the urn and braces forms are covered by tests and return 404 live. `lib/api.ts` now throws `ApiError` carrying the HTTP status; AnalysisProgress treats a 404 as terminal, stops polling, and shows an "Analysis not found" card. The strict `get_session` path (POST question/resume) was deliberately left unchanged, so a malformed id there still 500s — those routes are only reachable from the owner's own UI with a real id.

**2026-09-22 | Pause question/options never persisted to Supabase, so no client could see what a paused pipeline was asking — FIXED (backend half; committed in d5e495b)**
Originally recorded as a known gap, not in errors.md, but in decisions.md 2026-05-18 and docs/infrastructure.md:78 ("Known gap"). `domain_pause_data` / `missing_value_pause_data` / `outlier_pause_data` lived only in LangGraph state, so GET /status could return only the pause status name.
Root cause: no column held the question; the pause-wait nodes wrote only `status` and cleared `user_pause_response`.
Solution: migration `supabase/migrations/20260922035037_add_pause_data_to_analyses.sql` (committed ba2f089, then applied; introspection confirms `pause_data jsonb` nullable at ordinal 21, 21 columns, 0 non-null rows). Both pause-wait nodes write `pause_data` in the same single update that sets the pause status. GET /status returns it only while the status is a pause value. POST /resume validates the answer against it and clears it in the same update that restores the status. Details in decisions.md 2026-09-22. The frontend still shows the static placeholder by design; the pause UI is a separate later build, and it is blocked on the three entries below.

**2026-09-22 | A domain-pause answer is never consumed — the Profiler reruns blind and the pipeline can continue with no profile — pre-existing, FIXED in bb0f3ed (Build F1)**
Mechanism: after POST /resume, `domain_pause_wait_node` returns the answer in `user_pause_response` and the graph re-enters `profiler_node`. But `build_profiler_message(df, context)` takes neither the answer nor anything derived from it, so the Profiler gets byte-identical input and will likely emit `domain_confirmation_required` again. `route_after_profiler` case (b) then routes to `clear_and_proceed` → Cleaner with `profile_report=None`, every `profiler_*` state field None, and no `profile_report` ever saved to Supabase (the pause branch returns before the save). A "confirm" or "correct: the domain is X" answer therefore never reaches any agent. If the rerun happens to score ≥80, the pipeline proceeds normally, but it still ignores a corrected domain.
Fix direction: pass the domain answer into the Profiler's message (and adopt a corrected domain as the hypothesis), which means editing profiler_system.md. It needs its own live-validated pass. Required before the frontend pause UI is meaningful.
Status update (2026-09-22, Build F1; committed in bb0f3ed): **code FIXED and unit-tested; live validation INCOMPLETE.** The wait node carries the answered question forward in state (`answered_domain_pause`), `profiler_node` sends a `domain_resolution` block to one resume call, provenance is re-derived by a normal Step 1, Python makes the settled domain authoritative and records `profile_report.domain_resolution`, and a repeat pause on a resume call raises SYSTEM_ERROR instead of reaching the Cleaner with no profile (route case (b) now raises too). Design in decisions.md 2026-09-22 (Build F1). Full suite 312 passed / 16 skipped; 13 of 13 mutations caught. Not closed: the Tier 1 live run could not reach a real pause (see the gate-bypass entry below), so no resume path has run against a real model yet.
Status update (2026-09-25, Tier B): **CLOSED — FIXED and fully live-validated.** Both resume paths ran against the real model: confirm settled "unknown" and kept score 22; correct settled the user's domain. Each saved a profile with re-derived provenance and a `domain_resolution` record, and neither re-paused. See decisions.md 2026-09-22 (Build F1 / Build F2), live validation updates.

**2026-09-22 | Pauses on more than one column likely loop forever: the Cleaner only ever sees the most recent answer — pre-existing, FIXED in bd70546 (Build F3)**
Mechanism: cleaner_system.md §8 emits one pause per column, sequentially. `cleaner_node` passes only `state["user_pause_response"]` (a single dict) to `build_cleaner_message`, and returns `user_pause_response: None` whenever it pauses again (cleaner.py `missing_value_decision_required` / `outlier_decision_required` branches). With tests/fixtures/messy_data.csv (`revenue` 35.0% missing, `notes` 45.5% missing): pause on `revenue` → answer → rerun → pause on `notes` → answer → the rerun sees only the `notes` answer → `revenue` is over 30% again with no answer in the message, and the prompt makes that pause mandatory → asks about `revenue` again, indefinitely. Nothing guards against it the way `route_after_profiler` case (b) guards the Profiler. Since the pause_data build, each repeated question is visible via GET /status instead of silent; the loop itself is unchanged.
Fix direction: accumulate answers (e.g. a list keyed by `pause_type` + `column_name`, which the resume contract now carries) and pass all of them to the Cleaner, plus prompt wording telling it to honor earlier answers. Needs a live-validated pass. Required before the frontend pause UI is meaningful.
Status update (2026-09-25, Build F3; committed in bd70546; live validation recorded below): **code FIXED and unit-tested.** Confirmed as logged, plus worse: the wait node discarded the answered question itself (options, method), and cleaner_system.md never said what `user_pause_response` meant. The wait node now appends every answered question and its answer to `answered_cleaner_pauses` (declared in PipelineState — LangGraph 0.2.0 silently drops undeclared keys, verified). The Cleaner receives all of them as `resolved_pauses`, and `validate_cleaner_pause` raises on a repeat of an answered (pause_type, column) pair instead of looping. See decisions.md 2026-09-25 (Build F3).
Status update (2026-09-25, F3 live on messy_data.csv): **live-validated for two pauses.** Revenue then notes were each asked exactly once and both answers were honored in the final run (C1 3d5c0c7c-5844-4d90-b3a9-49c32443b0ff, C2 c412a7a3-1429-4c74-a13a-96ad04b30483, C3 11b3300a-ca02-46cc-9fe7-d6fbbc769c34). The third planned pause (revenue outliers) never came: the model skipped it (entry "No Python backstop for the Cleaner's outlier pauses" below).
Status update (2026-09-25, Build F3b, committed in 35cbc68): **CLOSED — FIXED and fully live-validated.** F3b's resume from F3's saved state asked the third pause (revenue outliers, produced by the new backstop) exactly once, and the final run honored all three answers in order (impute, preserve_missingness, flag_as_suspected_error), each attributed to the user, with nothing asked twice (C3 115f25d2-9ac0-4b7b-a237-81323c7e3161, C4 a3140726-428c-4070-b6ce-719021ca0994). See decisions.md 2026-09-25 (Build F3b).

**2026-09-22 | Missing-value pause offers 3 options, but docs/agents/cleaner.md requires a 4th ("preserve this missingness pattern as an analytical finding") — spec drift, FIXED in bd70546 (Build F3)**
Mechanism: docs/agents/cleaner.md "Missingness Pattern Analysis" says to "Add a 4th option to every over-30% pause" (preserve, for systematic-temporal / systematic-by-subset missingness). backend/prompts/cleaner_system.md §8.1 instead demands "exactly three options in this exact order: impute, exclude_column, exclude_rows". The spec change (decisions.md 2026-05-01) never reached the prompt. Effect: a user can never choose to preserve a merge-artifact or temporal missingness pattern as a finding at a pause. The pause_data build is unaffected: `pause_data` is stored raw and resume validation reads option ids from the stored payload, so adding the option later needs no backend change. Fix direction: update cleaner_system.md §8.1 (and the pre-output self-check's option count), which is LLM-facing prompt work for the same live-validated pass as the two entries above.
Status update (2026-09-25, Build F3; committed in bd70546; live validation recorded below): **FIXED, with honest wording.** Every checked missing-value pause gets a fourth option, `preserve_missingness`, written by Python, not the model. The spec's "the pattern itself will be investigated by the Analyzer" was not adopted: no Analyzer code receives or investigates a preserved pattern, so the option says only what the pipeline does (values stay missing, statistics use the recorded values, the missingness is recorded in the cleaning report). /resume needed no change: it reads option ids from the stored question. See decisions.md 2026-09-25 (Build F3).
Status update (2026-09-25, F3 live): **live-validated.** At the backstop-generated notes pause the user chose `preserve_missingness`; the final data kept notes' 84 missing values (after dedupe) untouched and the report records it as the user's decision (C3 11b3300a-ca02-46cc-9fe7-d6fbbc769c34).

**2026-09-22 | Four bugs in the new POST /resume validation, found by report-only Code Review passes during the pause_data build — FIXED in the same build (not pre-existing)**
Introduced by this build's first implementation, and found before commit by Code Review (Rule 13):
1. **Stranding (medium).** `_validate_pause_response` built the allowed ids from the stored `pause_data.options[].id`. A stored question with no `options` key, an empty list, or options without ids gave `[]` or `[None]`, so every legitimate answer got a 400. The pause-wait node polls with no timeout, so the analysis would have stayed paused for good. This contradicted the second evaluation's claim that the null-`pause_data` escape hatch meant nothing could be stranded (corrected in decisions.md 2026-09-22). Fix: the escape hatch (pause_type-only check + warning) now covers every case with no usable non-empty string option id.
2. **KeyError → 500 (low).** With `None` among the allowed ids, a response with no `option_id` passed the membership check and then crashed on `response["option_id"]`. Fix: `response.get("option_id")`, and unusable ids are filtered out, so this line is now unreachable with a missing `option_id` (defense in depth).
3. **Stale resume answers a newer pause (medium as reported, low likelihood).** The update matched only `id`, so a resume validated against one pause could land after the Cleaner re-paused on another column: it would clear the new question and hand the old answer to the new pause. `.eq("status", status)` alone does not catch this, because the re-pause repeats `missing_value_pause`. Fix: the write also matches the `updated_at` read with the question, which the pause-wait node stamps atomically with each pause. Zero rows updated → 409 "no longer active", nothing written. supabase-py 2.28.3 `update()` defaults to `returning=representation` (Context7 + installed source), so `data` is `[]` on no match. The exact-timestamp filter was verified read-only against all 6 live rows.
4. **Escape hatch skipped `column_name` too (low; found by the final scoped Code Review of fixes 1-3).** An answer from a stale tab for another column was accepted whenever the stored option ids were unusable. The 409 cannot catch that, because it covers only the pause changing between this request's read and write — the client does not send which pause it answers, and the earlier code comment overstated this. Fix: inside the escape hatch, `column_name` is still enforced for missing-value and outlier pauses whenever the stored one is a non-empty string; only `option_id` is skipped. Comment, decisions.md and docs/infrastructure.md reworded to say what each check covers.
Tests: 14 net new in tests/test_api.py (4 unvalidatable shapes × accept/reject, the escape hatch rejecting a mismatched column and accepting a matching one for both cleaner pause types, missing option_id → 400, the conditional-write filters, the null-`updated_at` filter, stale → 409). Mutation check: 7 of 8 targeted breaks caught (including disabling the escape-hatch column check); the survivor, reverting fix 2 alone, is an equivalent mutant (unreachable once fix 1 filters unusable ids), and reverting fixes 1 and 2 together is caught. The original 7 breaks were re-run against the fixed code and are all still caught.

**2026-09-22 | Chart text has no chosen font — an open design question, not deferred work (recorded during the DM Sans fix)**
What is known: `backend/tools/viz_tools.py` sets no font family anywhere. The static PNG charts (matplotlib 3.8.0 + seaborn) take their font from `sns.set_style("whitegrid")`'s default sans list (`Arial, DejaVu Sans, Liberation Sans, …`). That resolves to Arial on this Mac and would be DejaVu Sans on a host without Arial. The interactive HTML charts (plotly 5.20.0: line chart and scatter plot) use plotly's default `"Open Sans", verdana, arial, sans-serif`; only the scatter title's size (14) is set. Plotly charts render in an `<iframe>` (ChartGrid.tsx), so page CSS never reaches them. No doc (ui-and-frontend.md, analyzer.md, decisions.md) specifies a chart font, and none was chosen to match the page. So chart text never matched the page font: before the DM Sans fix it sat next to the system font, and now it sits next to DM Sans. The fix did not create this difference, but may make it more noticeable.
What is not known or decided: whether chart text *should* match DM Sans at all. Charts are rendered server-side, may be read outside the page (downloaded PNGs), and would need the font installed or bundled on the server for matplotlib and loaded inside each plotly HTML file. No fix is implied or scheduled; this records the question so it is not mistaken for a decision. If anyone takes it up, the first step is a design decision, not code.


**2026-09-22 | The Profiler emitted a full ProfileReport at domain_confidence_score 35 instead of the pause signal — the <80 gate is enforced only by the prompt, never in Python — found during Build F1 live validation, FIXED in 216c6ab (Build F2)**
Evidence: Tier 1 call 1 on tests/fixtures/ambiguous_domain.csv (anonymized columns ref/period/grp/x1/x2/x3/cat, 40 rows), claude-sonnet-4-6, 11,224 input / 4,151 output tokens, stop_reason end_turn, LangSmith run 1d5b47c9-4fb9-4866-b579-3dc0ea1a2a5b (name "f1-pause"). Output: a complete ProfileReport with `domain_hypothesis: "unknown"`, `domain_confidence_score: 35`, `provenance_hypothesis: "system export"`; its first top concern even says the domain is "unresolvable at 35% confidence". profiler_system.md Step 2, Section 8 and Section 10 all require the pause signal below 80. `profiler_node` checks only `parsed.get("type") == "domain_confirmation_required"`, so a sub-80 ProfileReport is saved and sent to the Cleaner with no pause — silently. Every earlier live run was iris.csv at 97-99, so this was never observable before.
Cause: not verified. Candidates: the model treats an "unknown" domain as something a question cannot fix and proceeds (Step 2 allows "unknown"; nothing says "unknown" must still pause); or the Build F1 prompt edits (which mention skipping the gate when `domain_resolution` is present — absent in this call) weakened the gate. Telling these apart needs live A/B calls, which were deliberately not made (instruction: stop after a failed first call, no iterating).
Cause update (2026-09-22, Build F2, Tier 1b): the F1 prompt edits were **not needed** to produce this failure. The pre-F1 prompt (`bb0f3ed^`, confirmed the last unmodified version) on the same input produced the same result: a full ProfileReport, `domain_hypothesis: "unknown"`, score 35, no pause (claude-sonnet-4-6, 10,797 in / 4,122 out, LangSmith run e5d89b75-d2ce-40ef-a391-7e32dda16c93, "f2-1b-first-call"). That supports the first candidate. It is one sample, so it shows only that the failure reproduces without F1's wording, not that F1's wording has no effect. The diff did contain one real softening — Section 10 item 2 lost "only" — which F2 restored anyway (decisions.md 2026-09-22, Build F2). The architectural finding above (Python never enforced the gate) needed no correction.
Fix direction (its own evaluation): enforce the gate in Python, like `apply_computed_column_stats` — when no `domain_resolution` is present and a full ProfileReport arrives with `domain_confidence_score < 80`, convert it into a pause built from its own `domain_hypothesis`, `domain_confidence_score` and `domain_supporting_signals` with the standard confirm/correct options, instead of trusting the model to self-gate. Then re-run F1's Tier 1 (the fixture already produces a sub-80 score). Scope: this gap applies to every run, not only resumes; F1 makes no further live calls until F2 is fixed and evaluated.
Open design question for the F2 evaluation to resolve explicitly (no answer presupposed): what a domain-pause answer, and the resulting `domain_resolution`, should look like when the model's own hypothesis is "unknown" or genuinely unresolvable. "Confirm" of "unknown" settles nothing, so this may need a third response shape distinct from confirm/correct rather than being silently absorbed into either. It would touch the Section 8 pause options, /resume validation and `build_domain_resolution`.
Correction (Build F2): "confirm of 'unknown' settles nothing" was wrong. It settles that no domain will be supplied — a real answer, recorded durably as `domain_resolution.source = "user_confirmed"`, `domain = "unknown"`, and exactly the premise of the existing `"unknown"` branches in cleaner_system.md:148 and analyzer_system.md:171. No third response shape was needed (decisions.md 2026-09-22, Build F2).
Cost so far: 1 Profiler invocation, about $0.10 at $3/$15 per MTok (re-check current pricing).
Status update (2026-09-22, Build F2; committed in 216c6ab): **code FIXED and unit-tested.** `apply_confidence_gate` (profiler.py) runs on every first (non-resume) call, right after parsing and before the existing pause branch. It converts a full ProfileReport scored below 80 into the standard Section 8 pause (hypothesis text unchanged, `domain_supporting_signals` renamed to `supporting_signals`, exact `confirm`/`correct` ids), and raises SYSTEM_ERROR on a missing or unusable score. The call site is guarded by `domain_resolution is None`, so a resume is never re-gated. Section 10 item 2 got back its "only". 24 new test items; full suite 336 passed / 16 skipped; 10 of 10 mutations caught; Code Review found 1 issue (decimal score dropped on confirm), fixed. Live Tier A PASSED on a user-approved re-run after an Anthropic API 500: the model again emitted a full report ("unknown", 22), and the Python backstop converted it into a correct pause (LangSmith b66201f7-e548-4e8d-bce4-3a7aad090c9b). The prompt alone still does not self-gate on this input. Not closed: Tier B (F1's resumes, and confirming a resume does not re-gate) was not run for lack of API balance; see decisions.md 2026-09-22 (Build F2).
Status update (2026-09-25, Tier B): **CLOSED — FIXED and fully live-validated.** Both real resume calls (confirm at 22, correct at 71) went through without being re-gated, which confirms the resume guard live (LangSmith c989e8ac-f641-49bd-8fe4-e296143bcd7c and a68d2847-54a6-4825-82f3-9652830c28c8; $0.2009). See decisions.md 2026-09-22 (Build F2), live validation update.

**2026-09-22 | A domain pause whose options use ids other than "confirm"/"correct" is accepted by /resume but raises in the Profiler — found by Code Review during Build F1, NOT fixed (logged)**
/resume validates `option_id` against the stored (raw LLM) `pause_data.options[].id`; `build_domain_resolution` accepts only "confirm" and "correct" (the ids profiler_system.md Section 8 specifies exactly). If the model emits other ids, a user answer gets a 200 and then the pipeline ends in SYSTEM_ERROR. Same for a "confirm" when the stored pause has no `domain_hypothesis` (reachable via the escape hatch). Loud, not silent, and it requires the model to break the exact-id contract. Possible fix: map by the option's `action` ("proceed_with_hypothesis" / "request_user_specified_domain") as a fallback, or reject non-standard domain ids at /resume.

**2026-09-22 | cleaner_system.md says the Cleaner sees a "post-confirmation" domain_confidence_score, but build_cleaner_message never passes the score — spec/code mismatch, logged for Build F3, FIXED in bd70546**
cleaner_system.md:63 and :74 describe `profiler.domain_confidence_score` as a Cleaner input that "reflects the post-confirmation state". `build_cleaner_message` (cleaner.py) passes `domain_hypothesis`, `provenance_hypothesis`, `top_3_concerns` and a `profile_summary`, but no score. No behavior depends on it today. F3 (which owns cleaner.py and cleaner_system.md) should decide whether to pass it — and if so, note that since F1 a confirmed domain keeps its original sub-80 score, alongside `profile_report.domain_resolution`.
Status update (2026-09-25, Build F3; committed in bd70546): **FIXED by correcting the prompt, not by passing the score.** Since F1 the score is either the original sub-80 pause score or the resume call's own (the 22→71 calibration concern, errors.md 2026-09-25). build_cleaner_message now passes `profile_report.domain_resolution` instead, and cleaner_system.md §3 reads four keys plus `domain_resolution`. This also removes a latent trigger: §3 said to refuse the run if any of "five keys" was missing, and the score had never been sent.

**2026-09-22 | profiler_node has no stop_reason == "max_tokens" check — logged for future reference, NOT fixed**
The Profiler call is non-streaming with `max_tokens=8000` and never inspects `stop_reason`. A truncated response fails in `parse_json_response` as an opaque JSON error rather than a clear "truncated" SYSTEM_ERROR (the Analyzer and Explainer already check this). The F1 live call used 4,151 output tokens for a 7-column file, so wide files (the prompt profiles up to 50 columns) are a realistic truncation risk.

**2026-09-22 | The Profiler's output is never validated — the confidence gate was one instance of a pattern: mandatory prompt rules that Python never checks and that fail silently — found during the Build F2 evaluation, NOT fixed (logged only)**
profiler_system.md states these as required, and nothing in Python checks any of them. (1) `provenance_hypothesis` must be one of five exact labels (Step 1; Section 10 calls any other label a contract violation). The Cleaner and Analyzer prompts branch on the label, so a variant like "system export (ERP)" silently weakens that reasoning. (2) `top_3_concerns` / `top_3_patterns` must hold exactly three items. (3) "Every field above is required" (Step 7): a missing field becomes None downstream with no error. For example, `profiler_top_3_concerns` falls back to `or []` in analyzer.py:577. (4) The pydantic `ProfileReport` model (schemas.py:173) is stale — no domain, provenance or top-3 fields, and `columns` instead of `column_profiles` — and profiler.py imports it but never uses it to validate, so it gives false assurance. Already covered elsewhere: the gate (F2), score type (F2 raises on an unusable score), and non-standard pause option ids (entry above). Section 9's "mandatory" Memory MCP write cannot happen in a tool-free call, but profiler_node's `profiler_*` state fields stand in for it, so it is harmless. Fix direction (its own evaluation): a tolerant validation step after parsing, in the apply_computed_column_stats pattern — decide per field whether to raise, repair or warn — and either update or delete the stale pydantic model.

**2026-09-22 | F3 note: the Cleaner/Analyzer "unknown" domain branches describe a premise F2 now produces in a different way, and the Cleaner's branch will amplify the multi-column pause loop — logged for Build F3, FIXED in bd70546**
Since F2, a sub-80 "unknown" hypothesis is always paused, and a user who clicks confirm settles the domain as literally `"unknown"` (source `user_confirmed`). cleaner_system.md:148 and analyzer_system.md:171 already handle `"unknown"`, but describe it as occurring "only if the user explicitly refused domain confirmation". The behavior they prescribe (most conservative interpretation; no domain-specific lead weighting) fits. Only the premise wording is off: it should read that the user confirmed the domain cannot be named. Both branches are read by the LLM, not matched in Python (cleaner.py and analyzer.py only pass `domain_hypothesis` through), so there is no keyword fragility. Interaction to plan for: the Cleaner's branch says "default to pause for any decision a domain expert would want to make", so a confirmed-"unknown" run will pause more often. That directly exercises the multi-column pause loop (entry above). A full-pipeline run on tests/fixtures/ambiguous_domain.csv confirmed as "unknown" should not be attempted until F3 lands. F2's and F1's live validation stays Profiler-only for this reason. Owner: F3 (cleaner_system.md; analyzer_system.md wording could ride along).
Status update (2026-09-25, Build F3; committed in bd70546): **FIXED.** Both premises now read "which occurs when the user confirmed at the domain pause that the domain cannot be named". The Cleaner's "default to pause for any decision a domain expert would want to make" is narrowed to flag-and-surface, pausing only through the Section 8 signals under their own conditions: an unknown domain has no medical or financial outlier trigger, so "pause" could only produce a mislabeled outlier pause or extra missing-value pauses at 30% or less.

**2026-09-25 | Domain-resolution confidence score jumps sharply on "correct" with no new evidence — a live calibration concern, not a bug — found in Build F2 Tier B, NOT fixed (logged only)**
Evidence: the same tests/fixtures/ambiguous_domain.csv data scored `domain_confidence_score` 22 as "unknown" (Tier B Resume 1, confirm; LangSmith c989e8ac-f641-49bd-8fe4-e296143bcd7c; 22 is also Tier A's original pause score) and 71 for a human-supplied domain, "school classroom assessment records" (Resume 2, correct; LangSmith a68d2847-54a6-4825-82f3-9652830c28c8). That is a roughly 3x jump with zero change in the underlying evidence: the columns are still anonymized (ref/period/grp/x1/x2/x3/cat). The prompt defines the resume score, when the domain is corrected, as how strongly the data's own signals support the settled domain. A human supplying a domain should not change what the data itself supports.
Impact: none on correctness. The score gates nothing on a resume: F2's guard correctly never re-fires at either score, and on confirm Python restores the original score anyway. But 71 is a live instance of the authoritative-looking but unsupported confidence that Rule 11 exists to prevent, and downstream agents and the profile_report reader see it as a number.
Cause: not verified. It is a single sample. Plausible reading: the model scores how well the data fits the named domain, which is permissive for generic columns, rather than how much the data independently points to it.
Fix direction (when the Profiler's system-prompt scoring language gets its next real pass): tighten the resume-score wording so that data compatible with the domain is not evidence for it. Options: anchor it to the pre-resume score, or have Python keep the original score on correct as it already does on confirm, or store the model's score in a separately named field. Its own evaluation; no live calls implied.

**2026-09-25 | The model adopts specific, confident column interpretations once given a domain, beyond what anonymized column names can support — found in Build F2 Tier B, NOT fixed (logged only)**
Evidence: Tier B Resume 2 (correct → "school classroom assessment records"; LangSmith a68d2847-54a6-4825-82f3-9652830c28c8). `capability_assessment` labeled x1 a "continuous assessment score", x2 a "completion-rate" metric and x3 a "proficiency tier". These are specific, plausible-sounding meanings for columns that are anonymized and cannot be verified. The answer is not unhedged: the first `top_3_concerns` item correctly flags that the anonymized columns make it impossible to validate the statistics against the domain. But the capability_assessment wording reads more certain than the evidence supports, and it is what the Analyzer and Explainer consume when scoping their answers. (The third concern's "students who did not submit work" reading of the missing x2 values is similarly speculative, though it is framed as a possibility.)
Relation: same underlying pattern as the entry above, one layer downstream — in interpretation rather than in the confidence number itself.
Cause: not verified; single sample. Fix direction (together with the entry above, in the next Profiler prompt pass): when column names are uninformative, require that any domain-derived column meaning be labeled as an assumption (for example "x1 (assumed: assessment score)"), not stated as fact. Its own evaluation.

**2026-09-25 | The Cleaner's own (model-authored) decisions are still executed by keyword matching, so a model-written "exclude rows", "exclude column" or "remove outliers" can do nothing, or a different operation, while the cleaning report claims it — found in the Build F3 evaluation, FIXED in 042e798 (Build G)**
F3 fixed execution and reporting for USER-decided items only: `apply_user_decisions` executes each answer by option id and writes its decision record, and `filter_user_decided` removes the model's decisions on what the user decided. Every other decision is still the model's prose, routed by `classify_cleaning_decision` / `execute_cleaning_operations` (cleaner.py), where the first keyword match wins, and saved to `cleaning_report.decisions` / `cleaning_decisions` verbatim — whether or not anything ran.
Evidence (local, messy_data.csv, Build F3 evaluation VP2):
- "Exclude `notes` from analysis entirely" — the prompt's own option label, verbatim — ran **nothing**. The backtick defeats "exclude column" and "exclude from analysis". The column was kept, and `cleaner_excluded_columns` stayed empty, so the Analyzer analyzes a column the report says was excluded.
- "Exclude the 70 rows where `revenue` is missing" — the prompt's label, verbatim — ran **nothing**. "Removed the 70 rows where revenue is missing" ran **nothing** ("removed rows" does not contain "remove rows").
- "Exclude rows where revenue is missing" with the issue text "missingness means the sale was never recorded" ran a **mean fill** instead ("means" contains "mean", and the mean branch is checked before drop-rows): 70 NaN → 0, all rows kept.
- "Imputed missing values per user decision" ran **nothing** (no method word).
- "Flagged the 4 outlier values as suspected error; excluded from aggregate statistics" added a flag column only; **all 4 values stayed in every statistic**.
- "Removed the 4 outlier values pending review" ran **nothing, and logged no warning**: the outlier branch `continue`s when the text contains remove/delete/drop. **No code path removes outlier values at all.**
- Already logged (decisions.md 2026-09-19): an action containing "meaningless" is caught by the mean-fill branch.
- Build F3b live, C4 (2026-09-25, LangSmith a3140726-428c-4070-b6ce-719021ca0994), three contradictions in one report, all in model-authored decisions on columns the user did not decide: (a) region "normalised all region values to title case (North, South, East, West)" matched no rule and **ran nothing** ('SOUTH', 'north', 'south' remain), while a second region decision "filled 4 missing values with mode after normalisation" ran the mode fill; (b) return_flag: **cleaning changed data the model said it would leave alone.** The decision "flagged co-missingness pattern with 'notes'; no imputation applied to either column for the affected 15 rows; … filled with mode ('N')…" routed to a mode fill of **all 15** missing values (15 → 0), so the 15 values the model said it would not impute were imputed; (c) the dataset-level "no duplicate rows present; no action taken" again removed the **15 duplicate rows** (second live occurrence after F3 C3). None touched revenue or notes: `filter_user_decided` dropped the model's four decisions on those user-decided columns.
- Build F3b probe (2026-09-25): a flag-and-include decision written exactly as Step 8 requires — its issue states the SD distance "from the mean" — routes to a **mean fill**: on units_sold it filled all 16 missing values with the mean and added no flag, while the report says "flagged; included". Every compliant flag-and-include decision on a column with missing values does this. F3b's outlier records (computed from the cleaned data) now state the true state beside such a decision, but the fill still runs.
- Related crash found by F3's pure-move check (1,060 decision sets): "convert to int" followed by "set to 'unknown'" on the same column raises TypeError (Int64 fillna with a string) → SYSTEM_ERROR.
Why it matters: the report is the only record of what was done to the data (Rules 10 and 11, Honest Limitation); a claimed exclusion or removal that did not happen reaches the Analyzer, the Explainer and the screen as fact. F3's user-decided path shows the shape of the fix: a structured operation id in each decision (validated in Python, executed by id), and a decision record written from what actually ran. Its own evaluation: it changes the model's CleaningReport contract, so it is prompt-facing and needs live validation.
Live evidence (2026-09-25, F3 C3, LangSmith 11b3300a-ca02-46cc-9fe7-d6fbbc769c34): (a) the region decision "normalised all string values to title-case (e.g., 'north' → 'North', 'SOUTH' → 'South')" was routed to a **mode fill** (its issue text names the 4 missing values): the 4 NaN were filled, and 'SOUTH', 'north' and 'south' all remain beside 'North' and 'South' — no normalisation ran. (b) The dataset-level decision "no duplicate rows present; no action taken" (issue "0 exact duplicate rows present") has column_name null, so it routes to dedupe: **15 rows were actually removed** while the report says none were. The report is therefore wrong in both directions on one run.
Status update (2026-09-26, Build G; committed in 042e798): **code FIXED and tested; live validation recorded below when run.** The keyword router (`classify_cleaning_decision`, `execute_cleaning_operations`) is removed (approved under Rule 2, after the pure-move proof of the shared primitives). Every Cleaner decision now names an `operation` from a closed set — `convert_type`, `standardize_values`, `fill_missing`, `leave_missing`, `flag_outliers`, `note` — which Python validates without ever raising and runs in a fixed order; Python writes every record from what ran ("Cleaner decision:" with Python's counts, "Not executed:" with the reason, or "No data changed:"), and the model's text survives only as its reasoning. Exact duplicates are a system step with Python's own count, so "no duplicate rows present" can neither run a removal nor be printed; model removal of rows, columns or outlier values is refused (Step 9); text normalization runs as `standardize_values` with an explicit mapping. Every item of the evidence above is a test in tests/test_cleaner.py Group 12, including the TypeError crash (now a refusal), the lossy `to_datetime` and the fill-with-text on a numeric column. Full suite 525 passed / 16 skipped; 66 of 66 mutations caught; report-only Code Review passes, with findings fixed or logged. See decisions.md 2026-09-26 (Build G).
Status update (2026-09-26, Build G live R1; committed in 042e798): **live-validated, 22 of 22 checks** (LangSmith cb1e4375-73b3-43f4-b96b-876a5747bbe1; 21,077 in / 5,232 out; $0.1417). On messy_data.csv, resumed after F3b's three answers: all 15 model decisions named a valid operation and every executed record matched the saved data. The C3/C4 contradictions did not recur: region was standardized to 4 values by the model's mapping, return_flag's record says what ran (a mode fill, 15 values), and the 15 duplicates were removed by the system step with no "no duplicates" claim anywhere. The model still restated the user's choices in three decisions despite §8.4; the filter dropped them (decisions.md 2026-09-26, Build G, live validation).

**2026-09-25 | No Python backstop for the Cleaner's outlier pauses — deferred in Build F3, FIXED in 35cbc68 (Build F3b)**
F3 backstops the >30% missing-value pause (`apply_missingness_backstop`), because the trigger is a computable fact. The medical and financial outlier pauses were not backstopped: their trigger is a judgment about the domain (medical, financial, or financial-magnitude "by analogy" for retail/HR/marketing), which Python cannot compute without guessing at domain labels. A model that skips a mandatory outlier pause still proceeds silently. Possible direction: a narrow backstop when the settled domain is explicitly medical or financial and a numeric column has IQR outliers, as its own evaluation.
Status update (2026-09-25, F3 live): **now observed live — blocks F3's full validation.** On messy_data.csv ("Retail and E-commerce", score 87) the model never emitted the §8.3 financial-outlier pause for revenue, yet its C3 decision text claimed "financial-outlier pause emitted; outliers not modified pending user decision" (LangSmith 11b3300a-ca02-46cc-9fe7-d6fbbc769c34). The four outliers (500,000 to 750,000) are untouched and unflagged, and because `filter_user_decided` drops the model's decisions on revenue (the user decided its missing values), the final report says nothing about them at all. Third live instance of the F2 pattern: a mandatory prompt rule not followed (after F2's gate and C2's skipped notes pause). The narrow backstop is the next build; its evaluation follows the F3 commit.
Status update (2026-09-25, Build F3b): **CLOSED — FIXED and live-validated** (C3 115f25d2-9ac0-4b7b-a237-81323c7e3161: the model routed revenue financial without pausing and the backstop produced the pause; C4 a3140726-428c-4070-b6ce-719021ca0994; 18 of 18 checks). The fix: by model-written routing rather than domain matching (no closed domain list exists): the report's `outlier_review` routes each column with outliers medical / financial / none, Python converts a report that routed a column to a pause it did not emit into that pause, and every column with unanswered outliers gets a system record. It enforces the Cleaner's routing, not a domain truth: a financial column routed "none" is still not asked about, and the record says so. See decisions.md 2026-09-25 (Build F3b).

**2026-09-25 | analyzer.py never passes the cleaner.* inheritance keys to its LLM — pre-existing, found in the Build F3 evaluation, NOT fixed (needs sign-off: outside F3's scope)**
analyzer_system.md says the Analyzer reads `cleaner.excluded_columns`, `cleaner.outliers_handled` and `cleaner.user_decisions_incorporated` (and treats their absence as malformed input), but analyzer_node only logs them (analyzer.py:580-608). build_analyzer_message sends only `cleaning_report_summary` (decisions, summary, interactions, concerns). Since F3, the user's choices still reach the Analyzer through the attributed decision text, and excluded columns are absent from the parquet, so nothing F3 needs is lost. The structured keys are not passed.

**2026-09-25 | Two known side effects of user-chosen missing values — logged in Build F3, NOT changed**
(1) `re_profile_verification.passed` is false whenever any NaN remains (re_profile_dataframe), so a user's `preserve_missingness` or an excluded outlier (set to NaN) always reports "verification failed", although the NaN is intended. This is pessimistic, not a false success claim; the semantics predate F3. (2) `<column>_outlier_flag` (0/1) columns are classified as numeric by the Analyzer (classify_columns), so they get descriptive statistics and charts like a measurement — also true of the model's pre-existing OUTLIER FLAG branch.

**2026-09-25 | Counts in a Cleaner pause describe the raw upload, not the data after earlier answers or duplicate removal — found by Code Review during Build F3, NOT fixed (logged)**
Every Cleaner run reloads the raw upload, and the model's message is built from it, so the counts in a pause (the model's text and Python's overwritten `missing_count` / `outlier_count` / labels) describe the raw file. Execution happens later, after the model's own duplicate removal and the user's earlier choices. On messy_data.csv the notes pause says "its 91 missing values stay missing", while 7 of those 91 sit in duplicate rows, so the executed decision records 84. A user's earlier exclude-rows choice can likewise lower another column's missingness, even below 30%, and it would still be asked about. The executed decision record always states what actually ran, so nothing false is claimed after the fact; the question can state numbers that will not hold. Fix direction: compute pause counts, and the backstop's threshold check, on the frame after duplicate removal and the earlier answers — which needs the model's message built from that frame too, or the model's and Python's numbers diverge. Its own evaluation.
(c) Found in Build F3b: a model-emitted outlier pause's note text (`financial_context_note` / `clinical_significance_note`) keeps the model's own numbers, while Python now overwrites the pause's sd_distance, column_mean and column_std beside it (and outlier_value, unless the model's is one of the flagged values); an SD distance or mean in the note can disagree with the fields. Same reason as (b): the note is written in the domain's vocabulary.
Two consequences found by the second Code Review pass, same cause, same fix: (a) the "no recorded values" check that removes impute and exclude_rows also runs on the raw upload, so if an earlier exclude-rows answer leaves another column with no recorded values, its impute option is still offered and fails after the user answers (`Cannot impute …`), and its exclude-rows option would remove every remaining row with no error; (b) only the count-bearing labels are re-rendered — the model's `consequence` text for exclude_rows / exclude_column still carries the model's own counts, because it is written in the domain's vocabulary ("patients") and Python cannot template it without losing that. Not reachable without an exclude-rows answer.
Update (2026-09-26, Build G Code Review, finding 1; declined in the build): Build G makes duplicate removal an unconditional system step, so this drift is now deterministic whenever the upload has duplicates. Confirmed by the review: a column with 4 missing values, 2 of them in duplicated rows, is offered "Exclude the 4 rows" / "Impute the 4 missing values" and the records then say 2. Before Build G the same removal ran whenever the model wrote its Step 4 decision, which the prompt required, so this is not a regression. The fix (build the message, pauses, masks and the backstop threshold from the deduplicated frame) changes F3's pause counts and masks and is still its own evaluation. cleaner_system.md Step 4 now tells the model that `column_info` describes the data before duplicate removal.

**2026-09-25 | The IQR outlier bounds are computed in four places — found by Code Review during Build F3, NOT fixed (cleanup, logged)**
build_cleaner_message, detect_interactions, execute_cleaning_operations' OUTLIER FLAG branch and F3's `_iqr_outlier_mask` each compute Q1 − 1.5×IQR / Q3 + 1.5×IQR inline. F3 depends on `_iqr_outlier_mask` matching build_cleaner_message (the user answers about the values the model was shown); that holds by copy, and the "more than 4 recorded values" rule matches, but detect_interactions also skips `iqr == 0`. Tests pin the messy_data.csv count (4) in both. Fix: have the other three call `_iqr_outlier_mask` — a behavior-preserving refactor with its own pure-move check.
Update (2026-09-25, Build F3b): build_cleaner_message now calls `_iqr_bounds` / `_iqr_outlier_mask` (verified pure move), so the message, outlier_review, the pause and the records share one definition. Two copies remain: detect_interactions, and execute_cleaning_operations' OUTLIER FLAG branch for the model's own decisions, which computes bounds on the partly cleaned frame (after the model's fills and duplicate removal), so a model flag can mark different rows than the raw-upload mask the user and the outlier record refer to (Code Review, Build F3b); the record then says "…marked in `<col>_outlier_flag`, which marks N row(s) in all".
Update (2026-09-26, Build G): the model-decision OUTLIER FLAG copy went with the keyword router. The Cleaner's `flag_outliers` uses `_iqr_outlier_mask` on the raw upload, aligned by index label — the same mask as the outlier pause, the outlier records and the user's choice — so a model flag now marks exactly the rows the record and the user refer to. One copy remains, in detect_interactions, which is not an execution path.

**2026-09-25 | The Profiler reported duplicate_row_count 0 on a file with 15 exact duplicate rows — found in F3 live validation, FIXED (Build H, not yet committed)**
Evidence: F3's Profiler run on tests/fixtures/messy_data.csv (LangSmith 32da926b-e8f8-4d57-8d7d-989ebacdd9e8) saved `duplicate_row_count: 0`; `pd.read_csv(...).duplicated().sum()` is 15 (200 rows → 185). Downstream, the Cleaner's model repeated it ("0 exact duplicate rows present … no action taken") while Python's dedupe routing removed the 15 anyway (entry "The Cleaner's own (model-authored) decisions…" above), so the saved report contradicts the data.
Cause (known, not re-verified in code this session): the field has no Python-computed input — decisions.md 2026-09-17 (overwrite guarantee) deferred `duplicate_row_count` and the Step 4 structural fields as whole-table claims the model makes from `first_5_rows` and per-column samples. It is also one more instance of "The Profiler's output is never validated" (errors.md 2026-09-22).
Fix direction (its own build): compute `duplicate_row_count` in Python, send it in the message and overwrite the model's copy, in the `apply_computed_column_stats` pattern.
Update (2026-09-26, Build G): the Cleaner no longer depends on the Profiler's value. Its message carries Python's own `duplicate_row_count` (`df.duplicated().sum()`), never the Profiler's, and the system removes duplicates itself with that count. The saved `profile_report` still says 0 while the cleaning record says 15 until this task lands (tasks.md), which is a visible contradiction in the stored analysis.
Status update (2026-09-27, Build H; not yet committed): **code FIXED and tested; live validation recorded below when run.** `profiler_node` computes `int(df.duplicated().sum())` once on the full uploaded frame (every column, not the 50-column `df_subset` the model is shown), sends it as the message's top-level `duplicate_row_count`, and overwrites `parsed["duplicate_row_count"]` with it right after `apply_computed_column_stats`, before `apply_domain_resolution` and the single `profile_report` save — so a first run and a domain-pause resume both save Python's count. The definition and the frame are the Cleaner's (`remove_duplicate_rows`), and both loaders stringify labels the same way, so the profile and the cleaning record agree (a test loads each fixture through both loaders and compares). The same one-line expression, not an import: cleaner.py already imports profiler.py, so the reverse import would be circular. profiler_system.md Step 4 now tells the model to copy the count verbatim. Existing stored analyses keep their old value (no backfill). Also exposed by this fix: the whole `profile_report` is embedded in the Analyzer's and the Explainer's LLM messages (analyzer.py `build_analyzer_message`, explainer.py `build_explainer_message`), so until now both models read `duplicate_row_count: 0` beside a cleaning report that removed 15. 11 new test items in tests/test_profiler.py Group 14; full suite 536 passed / 16 skipped; 9 of 9 mutations caught. See decisions.md 2026-09-27 (Build H).
Status update (2026-09-27, Build H live; not yet committed): **live-validated, 4 of 4 checks** (LangSmith d9370b8b-1805-48b8-b595-e55e1cdfbd52; 11,754 in / 4,722 out; $0.1061). On messy_data.csv the saved count is 15, the model's own count before the overwrite was also 15, and no text claims there are no duplicates; a concern now names the 15 duplicate rows (7.5%).

**2026-09-25 | Columns after the 50th are invisible to the Cleaner's model — pre-existing limitation, found in the Build F3b evaluation, NOT fixed (logged)**
`build_cleaner_message` (cleaner.py) sends dtype, missingness, samples and outlier bounds only for `df.columns[:50]` ("First 50 included to prevent token bloat"). The model cannot decide, route or report anything on the rest: since Build F3b such a column gets no `outlier_review` entry, no outlier backstop and no outlier record, so its outliers are never mentioned. Yet `apply_missingness_backstop` scans every column, so a column after the 50th that is over 30% missing does pause, on Python-written text. The Profiler has the same 50-column cut (see the stop_reason entry above). Fix direction (its own evaluation): at minimum a dataset-level system record naming the columns after the 50th that have outliers or heavy missingness, or batching the message.

**2026-09-25 | compute_data_quality_score penalizes by searching decision text for "outlier" or "flag" — pre-existing keyword fragility, found in the Build F3b evaluation, NOT fixed (logged)**
analyzer.py `compute_data_quality_score` subtracts 0.1 once if any cleaning decision's `action` contains "outlier" or "flag" (lower-cased substring) — the same fragility as the Cleaner's keyword routing (entry "The Cleaner's own (model-authored) decisions…" above). It fires on "return_flag", on "no outliers found", on a user's `treat_as_valid`, and since Build F3b on every system outlier record ("Outlier review: …" / "Not reviewed: …"), so any dataset with IQR outliers the user did not answer now always takes the penalty, whatever was done about them. Fix direction (its own evaluation): derive the penalty from structured facts (`cleaning_report.outlier_review`, `outliers_handled`), not decision text.
Update (2026-09-26, Build G): every decision's `action` is now written by Python, so the keyword penalty reads Python's own text. It fires on a Cleaner `flag_outliers` record ("…`<col>_outlier_flag`…"), on F3b's outlier records ("Outlier review: …" / "Not reviewed: …"), on the user's outlier choices, and also on a refused flag request, whose reason names outliers ("`region` is not numeric, so it has no IQR outliers") — a penalty for an operation that did not run. The structured `cleaning_report.operations` log now makes the structural fix possible; it is analyzer.py work and needs sign-off.

**2026-09-26 | The Cleaner's model never received the Profiler's structural fields: build_cleaner_message read a `structural_observations` key the Profiler never writes — pre-existing, found in the Build G evaluation, FIXED in 042e798 (Build G), with the Profiler's pattern fields deliberately not sent**
Mechanism: `build_cleaner_message` sent `profile_summary.structural_observations = profile_report.get("structural_observations")`, but profiler_system.md's ProfileReport has no such key: `duplicate_row_count`, `semantically_categorical_columns`, `co_emptiness_patterns`, `co_completeness_patterns`, `default_value_frequencies` and `potential_merge_artifacts` are top-level keys. Confirmed on F3's saved Profiler state (.live/f3_P.json): the value sent was `None`. So Steps 1, 4, 5 and 6 of cleaner_system.md, which tell the model to read those fields, ran blind; the live "0 exact duplicate rows present" (F3 C3, F3b C4) was a claim with no input behind it, and customer_id's conversion came from the model's own reading of dtype and samples.
Resolution (S6, adjusted by the user before the build): the message now sends `semantically_categorical_columns` (the Profiler's judgment, which Build B's ID conversion relies on) and Python's own `duplicate_row_count` (never the Profiler's, which was 0 against 15). The four pattern fields are whole-table claims the Profiler makes from a sample (decisions.md 2026-09-17; the 2026-09-22 "The Profiler's output is never validated" entry) and are not sent. The check made before building: the Cleaner's own full-data analyses cover what Steps 1, 4 and 6 need. Co-emptiness is covered by `missingness_patterns` ("correlated-with-other-columns", always sent) and by `detect_interactions`' co-missing pairs, computed on every run since the start but never sent to the model, now sent as `interactions_detected`. Merge artifacts are covered by `missingness_patterns`' "systematic-by-subset" and "systematic-temporal". Not covered, and left out rather than sent as unverified observations: co-completeness (its only consequence is a note, and among columns with missing values it is the same fact as co-emptiness) and default-value frequencies (only a note; the model now sees real counts for every low-cardinality text column in `distinct_values`; numeric sentinels such as a 0 in 47% of rows are not covered). Computing the Profiler's structural fields in Python is a tasks.md line. See decisions.md 2026-09-26 (Build G).

**2026-09-26 | cleaning_report.profiler_concerns_addressed said `addressed: false` for every concern on every run — pre-existing, found in the Build G evaluation, MITIGATED in 042e798 (Build G): now "not assessed"; the real fix is its own build**
cleaner.py computed `addressed` by searching every decision's reason and issue for `str(concern)` — the whole Profiler concern object (a dict with issue, affected_columns and why_it_matters), which never occurs in any text. Live F3b C4: false, false, false. The value reaches the Analyzer's message (`cleaning_report_summary.profiler_concerns_addressed`) and the Explainer on every run, telling them the Cleaner addressed none of the Profiler's concerns. Meanwhile the model's own `profiler_concerns_addressed`, which the prompt requires, is discarded. Build G stops sending a value known to be wrong: each entry is now `{"concern": <the concern>, "addressed": "not assessed"}`, and the matching code is removed. Real fix (its own build): carry the model's own acknowledgments — but they are prose that can claim operations that did not run, so they need the same treatment as decisions, for example referencing decision numbers the system checks.

**2026-09-26 | The Analyzer recognizes a date column only by its dtype, and the Cleaner's prompt never asked for date conversion — observation, NOT fixed (logged)**
`classify_columns` (analyzer.py:106-127) takes the first column whose dtype name contains "datetime", and `pd.read_csv` loads dates as text, so time-series detection runs on a CSV upload only if the Cleaner converted the column. Before Build G that happened only when the model's prose happened to say "convert to datetime", and the keyword router's `to_datetime(errors="coerce")` then silently turned every unparseable value into NaT (a text column probed locally: 200 of 200 values erased). Build G makes `convert_type` to `datetime` available, lossless only, and Step 5 permits it for dates stored as text; nothing requires it. Whether the Analyzer should parse dates or the Cleaner be required to convert them is a separate decision.

**2026-09-26 | On a column whose outliers the user answered, the Cleaner's fill or leave_missing for its (≤30%) missing values is dropped, so those values stay missing and no record says so — found by Code Review during Build G, FIXED in 042e798 (Build G; approved by the user 2026-09-26)**
Build G's `filter_user_decided` keeps only a `note`, or a `flag_outliers` where the user answered only the missing-value pause, on any user-decided column (S5, approved as proposed). The review's case: `amount` is 8% missing and has outliers; the user answers only the outlier pause; the Cleaner's `fill_missing` on `amount` is dropped, so the gaps stay missing without anyone deciding it, and the only trace is a "dropped" entry in `cleaning_report.operations`. Not a regression (F3's filter dropped every decision on the column). Analysis for the sign-off: a `fill_missing` or `leave_missing` on a column where the user answered only the outlier pause looks safe to allow — fills run before the user's outlier choice, a filled row was missing in the raw upload so the raw-mask never marks it, and a later "exclude" choice sets the masked values to missing after the fill. `convert_type` and `standardize_values` should stay dropped (a conversion to text breaks the user's outlier choice). The review's second case (a user mode imputation on a text column whose case variants the Cleaner wanted to standardize first) is a genuine trade-off: standardizing first changes the mode the user was shown. Leave as is until decided.
Status update (2026-09-26, Build G; committed in 042e798): **approved and FIXED.** `filter_user_decided` now also keeps a `fill_missing` or `leave_missing` on a column where the user answered only the outlier pause (not a missing-value pause, not excluded), so its smaller gaps are handled and recorded. Confirmed first on the executor itself: fills run in `_MODEL_PHASES_BEFORE_USER`, before `apply_user_decisions`' outlier choices, so a fill touches only the originally missing values (on the test frame, the one gap filled with the median computed with the outliers still present, as when the user was asked) and the values the user excludes as outliers stay missing (their raw-upload mask never includes a missing value). A `leave_missing` there is re-checked by row, so the user's later exclusion is not a false failure. Unchanged: where the user answered the missing-value pause, the Cleaner's fill or `leave_missing` is still dropped; `convert_type` and `standardize_values` are still dropped on any user-decided column. Tests in tests/test_cleaner.py Group 13; four new mutants, including fills moved after the user's choices. A scoped report-only review of the change found one defect, fixed: because the fill runs first, its median, mean or mode was computed with the outliers the user then excludes as errors (on the test frame a mean fill would have written about 243 into a column whose real values run 10-20). Where the user excluded a column's outliers, the Cleaner's fill value is now computed without them, and the record says so ("computed without the N value(s) the user excluded as outliers"); where the user kept them as valid they count. Mutation check 66 of 66. The review's second case (standardizing a text column before the user's mode imputation) was not part of the approval and is unchanged. A confirmation pass on the fix found no problems, and noted one pre-existing inconsistency for a later decision: the user's own "impute" answer on a column whose outliers the same user excludes still computes its value with those outliers present. That is F3's deliberate order (decisions.md 2026-09-25, Build F3: the impute value was shown with the outliers present), and it now differs from the Cleaner's own fills.

**2026-09-27 | profile_report.data_quality_score is still written by the model and never checked in Python — found in the Build H evaluation, NOT fixed (logged)**
profiler_system.md Step 7 tells the model to compute `data_quality_score` itself, "from missingness rate, duplicate rate, and outlier prevalence", and place only the final number in the JSON. Python neither computes nor overwrites it, so the saved number is whatever the model produced; before Build H it was computed with the model's own guessed duplicate count (0 against 15 on messy_data.csv). No Python code reads this field by name — the `ProfileReport` pydantic model in schemas.py declares it but is never instantiated, and the API, frontend and Explainer's reliability language use the Analyzer's separate `analyses.data_quality_score` — but the whole `profile_report` is embedded in the Analyzer's and the Explainer's LLM messages (`build_analyzer_message`, `build_explainer_message`), so both models see it. Build H sends the model Python's duplicate count, so the score should now be computed from the right input, but nothing checks the arithmetic or the other two components. Another instance of "The Profiler's output is never validated" (2026-09-22). Fix direction (its own evaluation): compute it in Python from the full frame and overwrite the model's copy, or drop it from the Profiler's contract, since the Analyzer's score is the one shown.
Live evidence (2026-09-27, Build H, LangSmith d9370b8b-1805-48b8-b595-e55e1cdfbd52): with the correct count in its input the model scored messy_data.csv 0.58 (F3's pre-fix run: 0.61 with a count of 0). The prompt's own formula gives 0.93 on this file (1 − mean of the missing 0.126, duplicate 0.075 and outlier 0.008 rates), or 0.79 as 1 − their sum; 0.58 matches neither, so the number is not reproducible from the prompt's definition.

**2026-09-27 | The Analyzer's duplicate deduction in compute_data_quality_score now fires only on duplicates created by cleaning, not on uploaded ones, and nothing documents or tests that — found in the Build H evaluation, NOT fixed (logged)**
`compute_data_quality_score` (analyzer.py) subtracts 0.1 when `df.duplicated().any()` on the cleaned parquet. Since Build G the Cleaner removes every exact duplicate of the upload as its first, unconditional step, so the condition the deduction was written for (the uploaded data had duplicate rows) never reaches it. It can still fire when a cleaning operation makes two distinct rows identical — for example a fill or a `standardize_values` mapping that equalizes the only column in which two rows differed. That is a different signal ("cleaning collapsed distinct records"), with no record in the cleaning report, no docstring and no test. Fix direction (its own evaluation, analyzer.py, needs sign-off): decide whether that case should be penalized, reported, or prevented; if the upload's duplicates are meant to count, read them from `cleaning_report.operations` (the system's `remove_duplicates` entry) instead of the cleaned frame.
